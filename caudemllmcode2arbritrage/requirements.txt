# requirements.txt
# All project dependencies — pinned versions for reproducibility
# Install with: pip install -r requirements.txt

# ── Web3 / Blockchain ─────────────────────────────────────────────────────────
web3==6.11.4
eth-account==0.9.0
eth-typing==3.5.2
hexbytes==0.3.1

# ── HTTP / API ────────────────────────────────────────────────────────────────
requests==2.31.0
aiohttp==3.9.1              # Async HTTP (used later for concurrent price feeds)

# ── Environment / Config ─────────────────────────────────────────────────────
python-dotenv==1.0.0

# ── Data / Maths ──────────────────────────────────────────────────────────────
pandas==2.1.4
numpy==1.26.3

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==7.4.4
pytest-cov==4.1.0

# ── Utilities ─────────────────────────────────────────────────────────────────
colorama==0.4.6             # Coloured terminal output on Windows
python-dateutil==2.8.2
