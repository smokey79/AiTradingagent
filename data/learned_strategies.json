[
  {
    "id": "STRAT_LUX_BTC_USDT_1788369608",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "symbol": "BTC/USDT",
    "channel_source": "LuxAlgo",
    "video_url": "https://youtube.com/@LuxAlgo",
    "target_win_rate_pct": 76.5,
    "gate_68_met": true,
    "leverage": "5X Futures",
    "leverage_multiplier": 5.0,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "entry_conditions": [
      "SMC Liquidity Sweep confirmed on BTC/USDT (15m/1h)",
      "LuxAlgo Order Block retest with Bullish confirmation signal",
      "Volume Expansion > 1.45x 20-SMA & Money Flow positive"
    ],
    "risk_management": {
      "take_profit_pct": 4.0,
      "take_profit_leveraged_pct": 20.0,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-09-02T17:20:08.865752+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY"
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1788329243",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4.0,
      "take_profit_leveraged_pct": 20.0,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-09-02T06:07:23.971106+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1788307254",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4.0,
      "take_profit_leveraged_pct": 20.0,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-09-02T00:00:54.968546+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1788306988",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4.0,
      "take_profit_leveraged_pct": 20.0,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-09-01T23:56:28.480015+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1788306938",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4.0,
      "take_profit_leveraged_pct": 20.0,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-09-01T23:55:38.815944+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1788268255",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4.0,
      "take_profit_leveraged_pct": 20.0,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-09-01T13:10:55.092708+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_SMC_LUXALGO_5X_BTC_USDT_15m_164827",
    "title": "LuxAlgo SMC & 5X Futures Leverage Strategy (BTC/USDT 15m)",
    "name": "LuxAlgo SMC & 5X Futures Leverage Strategy (BTC/USDT 15m)",
    "type": "smc_luxalgo_5x",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X",
    "channel_source": "LuxAlgo Official & YouTube Alpha",
    "target_win_rate_pct": 0,
    "concepts": [
      "Order Block (OB) Identification",
      "Liquidity Sweep Invalidation",
      "Dynamic ATR Trailing Stop (1.5x)",
      "Isolated 5X Leverage Margin"
    ],
    "risk_management": {
      "take_profit_pct": 4.0,
      "take_profit_leveraged_pct": 20.0,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.5
    },
    "pinescript_code": "//@version=5\nstrategy(\"AiTradingAgent \u2014 LuxAlgo SMC & 5X Futures Strategy (BTC/USDT)\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05, process_orders_on_close=true)\n\n// ==============================================================================\n// AiTradingAgent v4 \u2014 Autonomous PineScript v5 Strategy\n// Symbol: BTC/USDT | Leverage: 5X Isolated Margin\n// Target Win Rate: >68.0% | Risk-Reward Ratio: 2.33:1\n// Concepts: LuxAlgo Order Blocks (OB), Liquidity Sweeps, Fair Value Gaps (FVG)\n// ==============================================================================\n\n// \u2500\u2500\u2500 Input Configuration \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(8, \"Order Block Lookback Period\", minval=3, maxval=50, group=grp_smc)\nuse_fvg      = input.bool(true, \"Enable Fair Value Gap (FVG) Confirmation\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity Multiplier\", minval=0.5, step=0.1, group=grp_smc)\n\ngrp_momentum = \"Oscillators & Volume Regime\"\nrsi_len      = input.int(14, \"RSI Calculation Length\", minval=2, group=grp_momentum)\nrsi_long_min = input.int(45, \"RSI Long Minimum (Expansion Gate)\", minval=30, maxval=70, group=grp_momentum)\nrsi_long_max = input.int(65, \"RSI Long Maximum (Overbought Gate)\", minval=50, maxval=90, group=grp_momentum)\nrsi_short_min= input.int(32, \"RSI Short Minimum (Oversold Gate)\", minval=10, maxval=50, group=grp_momentum)\nrsi_short_max= input.int(55, \"RSI Short Maximum (Compression Gate)\", minval=30, maxval=70, group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index (MFI) Length\", minval=5, group=grp_momentum)\nvol_mult     = input.float(1.35, \"Institutional Volume Multiplier\", minval=1.0, step=0.05, group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5, \"Futures Leverage Multiplier (Isolated)\", minval=1.0, maxval=20.0, step=0.5, group=grp_futures)\ntp_pct_raw   = input.float(3.5, \"Underlying Take-Profit %\", minval=0.5, step=0.1, group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Underlying Stop-Loss %\", minval=0.5, step=0.1, group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", minval=5, group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Stop Multiplier\", minval=0.5, step=0.1, group=grp_futures)\nuse_trailing = input.bool(true, \"Enable Dynamic ATR Trailing Stop\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\nema_20  = ta.ema(close, 20)\nema_50  = ta.ema(close, 50)\nema_200 = ta.ema(close, 200)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nvol_surge     = volume > (vol_ma * vol_mult)\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and vol_surge\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and vol_surge\n\n// Fair Value Gap (FVG) Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum & Trend Confluence\nbull_momentum = (rsi_v >= rsi_long_min and rsi_v <= rsi_long_max) and (mfi_v >= 50.0)\nbear_momentum = (rsi_v <= rsi_short_max and rsi_v >= rsi_short_min) and (mfi_v <= 50.0)\ntrend_bullish = close > ema_20 and ema_20 > ema_50\ntrend_bearish = close < ema_20 and ema_20 < ema_50\n\n// Entry Trigger Logic\nlong_condition  = (bullish_sweep or (bullish_fvg and trend_bullish)) and bull_momentum and ta.crossover(close, ema_20)\nshort_condition = (bearish_sweep or (bearish_fvg and trend_bearish)) and bear_momentum and ta.crossunder(close, ema_20)\n\n// \u2500\u2500\u2500 Strategy Execution & Position Management \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl_price  = na\nvar float long_tp_price  = na\nvar float short_sl_price = na\nvar float short_tp_price = na\n\nif (long_condition and strategy.position_size == 0)\n    long_sl_price := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp_price := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"LONG\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl_price) + ',\"tp\":' + str.tostring(long_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_LONG\", strategy.long, alert_message=alert_json)\n\nif (short_condition and strategy.position_size == 0)\n    short_sl_price := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp_price := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"SHORT\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl_price) + ',\"tp\":' + str.tostring(short_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_SHORT\", strategy.short, alert_message=alert_json)\n\n// Dynamic ATR Trailing Stops & Limit Exits\nif (strategy.position_size > 0)\n    if (use_trailing)\n        long_sl_price := math.max(nz(long_sl_price, close - (atr_v * atr_sl_mult)), close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"SMC_5X_LONG\", stop=long_sl_price, limit=long_tp_price)\n\nif (strategy.position_size < 0)\n    if (use_trailing)\n        short_sl_price := math.min(nz(short_sl_price, close + (atr_v * atr_sl_mult)), close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SMC_5X_SHORT\", stop=short_sl_price, limit=short_tp_price)\n\n// \u2500\u2500\u2500 Visual Plots & Chart Overlays \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ema_20, \"EMA 20\", color=color.new(#3b82f6, 0), linewidth=1)\nplot(ema_50, \"EMA 50\", color=color.new(#8b5cf6, 0), linewidth=2)\nplot(ema_200, \"EMA 200\", color=color.new(#f59e0b, 0), linewidth=2)\n\nplotshape(long_condition and strategy.position_size == 0, title=\"SMC Long Signal\", location=location.belowbar, color=color.new(#10b981, 0), style=shape.triangleup, size=size.normal, text=\"5X BUY\")\nplotshape(short_condition and strategy.position_size == 0, title=\"SMC Short Signal\", location=location.abovebar, color=color.new(#ef4444, 0), style=shape.triangledown, size=size.normal, text=\"5X SELL\")\nplotshape(bullish_sweep, title=\"Bullish Liquidity Sweep\", location=location.belowbar, color=color.new(#06b6d4, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\nplotshape(bearish_sweep, title=\"Bearish Liquidity Sweep\", location=location.abovebar, color=color.new(#f97316, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\n",
    "pinescript": "//@version=5\nstrategy(\"AiTradingAgent \u2014 LuxAlgo SMC & 5X Futures Strategy (BTC/USDT)\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05, process_orders_on_close=true)\n\n// ==============================================================================\n// AiTradingAgent v4 \u2014 Autonomous PineScript v5 Strategy\n// Symbol: BTC/USDT | Leverage: 5X Isolated Margin\n// Target Win Rate: >68.0% | Risk-Reward Ratio: 2.33:1\n// Concepts: LuxAlgo Order Blocks (OB), Liquidity Sweeps, Fair Value Gaps (FVG)\n// ==============================================================================\n\n// \u2500\u2500\u2500 Input Configuration \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(8, \"Order Block Lookback Period\", minval=3, maxval=50, group=grp_smc)\nuse_fvg      = input.bool(true, \"Enable Fair Value Gap (FVG) Confirmation\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity Multiplier\", minval=0.5, step=0.1, group=grp_smc)\n\ngrp_momentum = \"Oscillators & Volume Regime\"\nrsi_len      = input.int(14, \"RSI Calculation Length\", minval=2, group=grp_momentum)\nrsi_long_min = input.int(45, \"RSI Long Minimum (Expansion Gate)\", minval=30, maxval=70, group=grp_momentum)\nrsi_long_max = input.int(65, \"RSI Long Maximum (Overbought Gate)\", minval=50, maxval=90, group=grp_momentum)\nrsi_short_min= input.int(32, \"RSI Short Minimum (Oversold Gate)\", minval=10, maxval=50, group=grp_momentum)\nrsi_short_max= input.int(55, \"RSI Short Maximum (Compression Gate)\", minval=30, maxval=70, group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index (MFI) Length\", minval=5, group=grp_momentum)\nvol_mult     = input.float(1.35, \"Institutional Volume Multiplier\", minval=1.0, step=0.05, group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5, \"Futures Leverage Multiplier (Isolated)\", minval=1.0, maxval=20.0, step=0.5, group=grp_futures)\ntp_pct_raw   = input.float(3.5, \"Underlying Take-Profit %\", minval=0.5, step=0.1, group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Underlying Stop-Loss %\", minval=0.5, step=0.1, group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", minval=5, group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Stop Multiplier\", minval=0.5, step=0.1, group=grp_futures)\nuse_trailing = input.bool(true, \"Enable Dynamic ATR Trailing Stop\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\nema_20  = ta.ema(close, 20)\nema_50  = ta.ema(close, 50)\nema_200 = ta.ema(close, 200)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nvol_surge     = volume > (vol_ma * vol_mult)\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and vol_surge\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and vol_surge\n\n// Fair Value Gap (FVG) Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum & Trend Confluence\nbull_momentum = (rsi_v >= rsi_long_min and rsi_v <= rsi_long_max) and (mfi_v >= 50.0)\nbear_momentum = (rsi_v <= rsi_short_max and rsi_v >= rsi_short_min) and (mfi_v <= 50.0)\ntrend_bullish = close > ema_20 and ema_20 > ema_50\ntrend_bearish = close < ema_20 and ema_20 < ema_50\n\n// Entry Trigger Logic\nlong_condition  = (bullish_sweep or (bullish_fvg and trend_bullish)) and bull_momentum and ta.crossover(close, ema_20)\nshort_condition = (bearish_sweep or (bearish_fvg and trend_bearish)) and bear_momentum and ta.crossunder(close, ema_20)\n\n// \u2500\u2500\u2500 Strategy Execution & Position Management \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl_price  = na\nvar float long_tp_price  = na\nvar float short_sl_price = na\nvar float short_tp_price = na\n\nif (long_condition and strategy.position_size == 0)\n    long_sl_price := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp_price := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"LONG\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl_price) + ',\"tp\":' + str.tostring(long_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_LONG\", strategy.long, alert_message=alert_json)\n\nif (short_condition and strategy.position_size == 0)\n    short_sl_price := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp_price := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"SHORT\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl_price) + ',\"tp\":' + str.tostring(short_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_SHORT\", strategy.short, alert_message=alert_json)\n\n// Dynamic ATR Trailing Stops & Limit Exits\nif (strategy.position_size > 0)\n    if (use_trailing)\n        long_sl_price := math.max(nz(long_sl_price, close - (atr_v * atr_sl_mult)), close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"SMC_5X_LONG\", stop=long_sl_price, limit=long_tp_price)\n\nif (strategy.position_size < 0)\n    if (use_trailing)\n        short_sl_price := math.min(nz(short_sl_price, close + (atr_v * atr_sl_mult)), close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SMC_5X_SHORT\", stop=short_sl_price, limit=short_tp_price)\n\n// \u2500\u2500\u2500 Visual Plots & Chart Overlays \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ema_20, \"EMA 20\", color=color.new(#3b82f6, 0), linewidth=1)\nplot(ema_50, \"EMA 50\", color=color.new(#8b5cf6, 0), linewidth=2)\nplot(ema_200, \"EMA 200\", color=color.new(#f59e0b, 0), linewidth=2)\n\nplotshape(long_condition and strategy.position_size == 0, title=\"SMC Long Signal\", location=location.belowbar, color=color.new(#10b981, 0), style=shape.triangleup, size=size.normal, text=\"5X BUY\")\nplotshape(short_condition and strategy.position_size == 0, title=\"SMC Short Signal\", location=location.abovebar, color=color.new(#ef4444, 0), style=shape.triangledown, size=size.normal, text=\"5X SELL\")\nplotshape(bullish_sweep, title=\"Bullish Liquidity Sweep\", location=location.belowbar, color=color.new(#06b6d4, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\nplotshape(bearish_sweep, title=\"Bearish Liquidity Sweep\", location=location.abovebar, color=color.new(#f97316, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\n",
    "learned_at": "2026-08-31T15:29:24.827Z",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {
      "strategyType": "smc_luxalgo_5x",
      "symbol": "BTC/USDT",
      "timeframe": "15m",
      "leverage": "5X",
      "initialCapital": 1000,
      "finalCapital": 986.41,
      "netProfitUsd": -13.59,
      "netProfitPct": -1.36,
      "totalTrades": 5,
      "winCount": 0,
      "lossCount": 5,
      "winRate": 0,
      "profitFactor": 0,
      "maxDrawdownUsd": 13.59,
      "maxDrawdownPct": 1.36,
      "sharpeRatio": -24.7,
      "sortinoRatio": -15.71,
      "riskRewardRatio": 0,
      "kellyOptimalFractionPct": 0,
      "gate68Met": false,
      "parameters": {
        "obLookback": 8,
        "rsiLongMin": 45,
        "rsiLongMax": 65,
        "volMultiplier": 1.35,
        "takeProfitPct": 3.5,
        "stopLossPct": 1.5,
        "atrMultiplier": 1.5,
        "symbol": "BTC/USDT",
        "timeframe": "15m"
      },
      "trades": [
        {
          "tradeNum": 1,
          "symbol": "BTC/USDT",
          "side": "SELL",
          "entryTime": "2026-08-29T05:15:00.000Z",
          "exitTime": "2026-08-29T06:00:00.000Z",
          "entryPrice": 77660.01,
          "exitPrice": 77608.1,
          "collateralUsd": 200,
          "positionSizeUsd": 1000,
          "leverage": 5,
          "pnlUsd": -0.83,
          "pnlPct": -0.42,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 3
        },
        {
          "tradeNum": 2,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-29T21:45:00.000Z",
          "exitTime": "2026-08-30T01:00:00.000Z",
          "entryPrice": 78161.63,
          "exitPrice": 78146.485,
          "collateralUsd": 199.83,
          "positionSizeUsd": 999.17,
          "leverage": 5,
          "pnlUsd": -1.69,
          "pnlPct": -0.85,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 13
        },
        {
          "tradeNum": 3,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-30T05:00:00.000Z",
          "exitTime": "2026-08-30T07:30:00.000Z",
          "entryPrice": 78146.01,
          "exitPrice": 78176.945,
          "collateralUsd": 199.5,
          "positionSizeUsd": 997.48,
          "leverage": 5,
          "pnlUsd": -1.1,
          "pnlPct": -0.55,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 10
        },
        {
          "tradeNum": 4,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-30T18:15:00.000Z",
          "exitTime": "2026-08-30T19:45:00.000Z",
          "entryPrice": 79009.61,
          "exitPrice": 78752.785,
          "collateralUsd": 199.27,
          "positionSizeUsd": 996.37,
          "leverage": 5,
          "pnlUsd": -4.73,
          "pnlPct": -2.38,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 6
        },
        {
          "tradeNum": 5,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-31T12:00:00.000Z",
          "exitTime": "2026-08-31T12:15:00.000Z",
          "entryPrice": 78410.01,
          "exitPrice": 78114.075,
          "collateralUsd": 198.33,
          "positionSizeUsd": 991.64,
          "leverage": 5,
          "pnlUsd": -5.23,
          "pnlPct": -2.64,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 1
        }
      ]
    },
    "parameters": {
      "obLookback": 8,
      "rsiLongMin": 45,
      "rsiLongMax": 65,
      "volMultiplier": 1.35,
      "takeProfitPct": 3.5,
      "stopLossPct": 1.5,
      "atrMultiplier": 1.5,
      "symbol": "BTC/USDT",
      "timeframe": "15m"
    }
  },
  {
    "id": "STRAT_SMC_LUXALGO_5X_BTC_USDT_15m_694329",
    "title": "LuxAlgo SMC & 5X Futures Leverage Strategy (BTC/USDT 15m)",
    "name": "LuxAlgo SMC & 5X Futures Leverage Strategy (BTC/USDT 15m)",
    "type": "smc_luxalgo_5x",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X",
    "channel_source": "LuxAlgo Official & YouTube Alpha",
    "target_win_rate_pct": 0,
    "concepts": [
      "Order Block (OB) Identification",
      "Liquidity Sweep Invalidation",
      "Dynamic ATR Trailing Stop (1.5x)",
      "Isolated 5X Leverage Margin"
    ],
    "risk_management": {
      "take_profit_pct": 4.0,
      "take_profit_leveraged_pct": 20.0,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.5
    },
    "pinescript_code": "//@version=5\nstrategy(\"AiTradingAgent \u2014 LuxAlgo SMC & 5X Futures Strategy (BTC/USDT)\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05, process_orders_on_close=true)\n\n// ==============================================================================\n// AiTradingAgent v4 \u2014 Autonomous PineScript v5 Strategy\n// Symbol: BTC/USDT | Leverage: 5X Isolated Margin\n// Target Win Rate: >68.0% | Risk-Reward Ratio: 2.33:1\n// Concepts: LuxAlgo Order Blocks (OB), Liquidity Sweeps, Fair Value Gaps (FVG)\n// ==============================================================================\n\n// \u2500\u2500\u2500 Input Configuration \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(8, \"Order Block Lookback Period\", minval=3, maxval=50, group=grp_smc)\nuse_fvg      = input.bool(true, \"Enable Fair Value Gap (FVG) Confirmation\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity Multiplier\", minval=0.5, step=0.1, group=grp_smc)\n\ngrp_momentum = \"Oscillators & Volume Regime\"\nrsi_len      = input.int(14, \"RSI Calculation Length\", minval=2, group=grp_momentum)\nrsi_long_min = input.int(45, \"RSI Long Minimum (Expansion Gate)\", minval=30, maxval=70, group=grp_momentum)\nrsi_long_max = input.int(65, \"RSI Long Maximum (Overbought Gate)\", minval=50, maxval=90, group=grp_momentum)\nrsi_short_min= input.int(32, \"RSI Short Minimum (Oversold Gate)\", minval=10, maxval=50, group=grp_momentum)\nrsi_short_max= input.int(55, \"RSI Short Maximum (Compression Gate)\", minval=30, maxval=70, group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index (MFI) Length\", minval=5, group=grp_momentum)\nvol_mult     = input.float(1.35, \"Institutional Volume Multiplier\", minval=1.0, step=0.05, group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5, \"Futures Leverage Multiplier (Isolated)\", minval=1.0, maxval=20.0, step=0.5, group=grp_futures)\ntp_pct_raw   = input.float(3.5, \"Underlying Take-Profit %\", minval=0.5, step=0.1, group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Underlying Stop-Loss %\", minval=0.5, step=0.1, group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", minval=5, group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Stop Multiplier\", minval=0.5, step=0.1, group=grp_futures)\nuse_trailing = input.bool(true, \"Enable Dynamic ATR Trailing Stop\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\nema_20  = ta.ema(close, 20)\nema_50  = ta.ema(close, 50)\nema_200 = ta.ema(close, 200)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nvol_surge     = volume > (vol_ma * vol_mult)\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and vol_surge\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and vol_surge\n\n// Fair Value Gap (FVG) Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum & Trend Confluence\nbull_momentum = (rsi_v >= rsi_long_min and rsi_v <= rsi_long_max) and (mfi_v >= 50.0)\nbear_momentum = (rsi_v <= rsi_short_max and rsi_v >= rsi_short_min) and (mfi_v <= 50.0)\ntrend_bullish = close > ema_20 and ema_20 > ema_50\ntrend_bearish = close < ema_20 and ema_20 < ema_50\n\n// Entry Trigger Logic\nlong_condition  = (bullish_sweep or (bullish_fvg and trend_bullish)) and bull_momentum and ta.crossover(close, ema_20)\nshort_condition = (bearish_sweep or (bearish_fvg and trend_bearish)) and bear_momentum and ta.crossunder(close, ema_20)\n\n// \u2500\u2500\u2500 Strategy Execution & Position Management \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl_price  = na\nvar float long_tp_price  = na\nvar float short_sl_price = na\nvar float short_tp_price = na\n\nif (long_condition and strategy.position_size == 0)\n    long_sl_price := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp_price := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"LONG\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl_price) + ',\"tp\":' + str.tostring(long_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_LONG\", strategy.long, alert_message=alert_json)\n\nif (short_condition and strategy.position_size == 0)\n    short_sl_price := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp_price := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"SHORT\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl_price) + ',\"tp\":' + str.tostring(short_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_SHORT\", strategy.short, alert_message=alert_json)\n\n// Dynamic ATR Trailing Stops & Limit Exits\nif (strategy.position_size > 0)\n    if (use_trailing)\n        long_sl_price := math.max(nz(long_sl_price, close - (atr_v * atr_sl_mult)), close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"SMC_5X_LONG\", stop=long_sl_price, limit=long_tp_price)\n\nif (strategy.position_size < 0)\n    if (use_trailing)\n        short_sl_price := math.min(nz(short_sl_price, close + (atr_v * atr_sl_mult)), close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SMC_5X_SHORT\", stop=short_sl_price, limit=short_tp_price)\n\n// \u2500\u2500\u2500 Visual Plots & Chart Overlays \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ema_20, \"EMA 20\", color=color.new(#3b82f6, 0), linewidth=1)\nplot(ema_50, \"EMA 50\", color=color.new(#8b5cf6, 0), linewidth=2)\nplot(ema_200, \"EMA 200\", color=color.new(#f59e0b, 0), linewidth=2)\n\nplotshape(long_condition and strategy.position_size == 0, title=\"SMC Long Signal\", location=location.belowbar, color=color.new(#10b981, 0), style=shape.triangleup, size=size.normal, text=\"5X BUY\")\nplotshape(short_condition and strategy.position_size == 0, title=\"SMC Short Signal\", location=location.abovebar, color=color.new(#ef4444, 0), style=shape.triangledown, size=size.normal, text=\"5X SELL\")\nplotshape(bullish_sweep, title=\"Bullish Liquidity Sweep\", location=location.belowbar, color=color.new(#06b6d4, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\nplotshape(bearish_sweep, title=\"Bearish Liquidity Sweep\", location=location.abovebar, color=color.new(#f97316, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\n",
    "pinescript": "//@version=5\nstrategy(\"AiTradingAgent \u2014 LuxAlgo SMC & 5X Futures Strategy (BTC/USDT)\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05, process_orders_on_close=true)\n\n// ==============================================================================\n// AiTradingAgent v4 \u2014 Autonomous PineScript v5 Strategy\n// Symbol: BTC/USDT | Leverage: 5X Isolated Margin\n// Target Win Rate: >68.0% | Risk-Reward Ratio: 2.33:1\n// Concepts: LuxAlgo Order Blocks (OB), Liquidity Sweeps, Fair Value Gaps (FVG)\n// ==============================================================================\n\n// \u2500\u2500\u2500 Input Configuration \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(8, \"Order Block Lookback Period\", minval=3, maxval=50, group=grp_smc)\nuse_fvg      = input.bool(true, \"Enable Fair Value Gap (FVG) Confirmation\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity Multiplier\", minval=0.5, step=0.1, group=grp_smc)\n\ngrp_momentum = \"Oscillators & Volume Regime\"\nrsi_len      = input.int(14, \"RSI Calculation Length\", minval=2, group=grp_momentum)\nrsi_long_min = input.int(45, \"RSI Long Minimum (Expansion Gate)\", minval=30, maxval=70, group=grp_momentum)\nrsi_long_max = input.int(65, \"RSI Long Maximum (Overbought Gate)\", minval=50, maxval=90, group=grp_momentum)\nrsi_short_min= input.int(32, \"RSI Short Minimum (Oversold Gate)\", minval=10, maxval=50, group=grp_momentum)\nrsi_short_max= input.int(55, \"RSI Short Maximum (Compression Gate)\", minval=30, maxval=70, group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index (MFI) Length\", minval=5, group=grp_momentum)\nvol_mult     = input.float(1.35, \"Institutional Volume Multiplier\", minval=1.0, step=0.05, group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5, \"Futures Leverage Multiplier (Isolated)\", minval=1.0, maxval=20.0, step=0.5, group=grp_futures)\ntp_pct_raw   = input.float(3.5, \"Underlying Take-Profit %\", minval=0.5, step=0.1, group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Underlying Stop-Loss %\", minval=0.5, step=0.1, group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", minval=5, group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Stop Multiplier\", minval=0.5, step=0.1, group=grp_futures)\nuse_trailing = input.bool(true, \"Enable Dynamic ATR Trailing Stop\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\nema_20  = ta.ema(close, 20)\nema_50  = ta.ema(close, 50)\nema_200 = ta.ema(close, 200)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nvol_surge     = volume > (vol_ma * vol_mult)\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and vol_surge\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and vol_surge\n\n// Fair Value Gap (FVG) Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum & Trend Confluence\nbull_momentum = (rsi_v >= rsi_long_min and rsi_v <= rsi_long_max) and (mfi_v >= 50.0)\nbear_momentum = (rsi_v <= rsi_short_max and rsi_v >= rsi_short_min) and (mfi_v <= 50.0)\ntrend_bullish = close > ema_20 and ema_20 > ema_50\ntrend_bearish = close < ema_20 and ema_20 < ema_50\n\n// Entry Trigger Logic\nlong_condition  = (bullish_sweep or (bullish_fvg and trend_bullish)) and bull_momentum and ta.crossover(close, ema_20)\nshort_condition = (bearish_sweep or (bearish_fvg and trend_bearish)) and bear_momentum and ta.crossunder(close, ema_20)\n\n// \u2500\u2500\u2500 Strategy Execution & Position Management \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl_price  = na\nvar float long_tp_price  = na\nvar float short_sl_price = na\nvar float short_tp_price = na\n\nif (long_condition and strategy.position_size == 0)\n    long_sl_price := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp_price := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"LONG\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl_price) + ',\"tp\":' + str.tostring(long_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_LONG\", strategy.long, alert_message=alert_json)\n\nif (short_condition and strategy.position_size == 0)\n    short_sl_price := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp_price := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"SHORT\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl_price) + ',\"tp\":' + str.tostring(short_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_SHORT\", strategy.short, alert_message=alert_json)\n\n// Dynamic ATR Trailing Stops & Limit Exits\nif (strategy.position_size > 0)\n    if (use_trailing)\n        long_sl_price := math.max(nz(long_sl_price, close - (atr_v * atr_sl_mult)), close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"SMC_5X_LONG\", stop=long_sl_price, limit=long_tp_price)\n\nif (strategy.position_size < 0)\n    if (use_trailing)\n        short_sl_price := math.min(nz(short_sl_price, close + (atr_v * atr_sl_mult)), close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SMC_5X_SHORT\", stop=short_sl_price, limit=short_tp_price)\n\n// \u2500\u2500\u2500 Visual Plots & Chart Overlays \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ema_20, \"EMA 20\", color=color.new(#3b82f6, 0), linewidth=1)\nplot(ema_50, \"EMA 50\", color=color.new(#8b5cf6, 0), linewidth=2)\nplot(ema_200, \"EMA 200\", color=color.new(#f59e0b, 0), linewidth=2)\n\nplotshape(long_condition and strategy.position_size == 0, title=\"SMC Long Signal\", location=location.belowbar, color=color.new(#10b981, 0), style=shape.triangleup, size=size.normal, text=\"5X BUY\")\nplotshape(short_condition and strategy.position_size == 0, title=\"SMC Short Signal\", location=location.abovebar, color=color.new(#ef4444, 0), style=shape.triangledown, size=size.normal, text=\"5X SELL\")\nplotshape(bullish_sweep, title=\"Bullish Liquidity Sweep\", location=location.belowbar, color=color.new(#06b6d4, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\nplotshape(bearish_sweep, title=\"Bearish Liquidity Sweep\", location=location.abovebar, color=color.new(#f97316, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\n",
    "learned_at": "2026-08-31T15:21:34.329Z",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {
      "strategyType": "smc_luxalgo_5x",
      "symbol": "BTC/USDT",
      "timeframe": "15m",
      "leverage": "5X",
      "initialCapital": 1000,
      "finalCapital": 986.41,
      "netProfitUsd": -13.59,
      "netProfitPct": -1.36,
      "totalTrades": 5,
      "winCount": 0,
      "lossCount": 5,
      "winRate": 0,
      "profitFactor": 0,
      "maxDrawdownUsd": 13.59,
      "maxDrawdownPct": 1.36,
      "sharpeRatio": -24.7,
      "sortinoRatio": -15.71,
      "riskRewardRatio": 0,
      "kellyOptimalFractionPct": 0,
      "gate68Met": false,
      "parameters": {
        "obLookback": 8,
        "rsiLongMin": 45,
        "rsiLongMax": 65,
        "volMultiplier": 1.35,
        "takeProfitPct": 3.5,
        "stopLossPct": 1.5,
        "atrMultiplier": 1.5,
        "symbol": "BTC/USDT",
        "timeframe": "15m"
      },
      "trades": [
        {
          "tradeNum": 1,
          "symbol": "BTC/USDT",
          "side": "SELL",
          "entryTime": "2026-08-29T05:15:00.000Z",
          "exitTime": "2026-08-29T06:00:00.000Z",
          "entryPrice": 77660.01,
          "exitPrice": 77608.1,
          "collateralUsd": 200,
          "positionSizeUsd": 1000,
          "leverage": 5,
          "pnlUsd": -0.83,
          "pnlPct": -0.42,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 3
        },
        {
          "tradeNum": 2,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-29T21:45:00.000Z",
          "exitTime": "2026-08-30T01:00:00.000Z",
          "entryPrice": 78161.63,
          "exitPrice": 78146.485,
          "collateralUsd": 199.83,
          "positionSizeUsd": 999.17,
          "leverage": 5,
          "pnlUsd": -1.69,
          "pnlPct": -0.85,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 13
        },
        {
          "tradeNum": 3,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-30T05:00:00.000Z",
          "exitTime": "2026-08-30T07:30:00.000Z",
          "entryPrice": 78146.01,
          "exitPrice": 78176.945,
          "collateralUsd": 199.5,
          "positionSizeUsd": 997.48,
          "leverage": 5,
          "pnlUsd": -1.1,
          "pnlPct": -0.55,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 10
        },
        {
          "tradeNum": 4,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-30T18:15:00.000Z",
          "exitTime": "2026-08-30T19:45:00.000Z",
          "entryPrice": 79009.61,
          "exitPrice": 78752.785,
          "collateralUsd": 199.27,
          "positionSizeUsd": 996.37,
          "leverage": 5,
          "pnlUsd": -4.73,
          "pnlPct": -2.38,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 6
        },
        {
          "tradeNum": 5,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-31T12:00:00.000Z",
          "exitTime": "2026-08-31T12:15:00.000Z",
          "entryPrice": 78410.01,
          "exitPrice": 78114.075,
          "collateralUsd": 198.33,
          "positionSizeUsd": 991.64,
          "leverage": 5,
          "pnlUsd": -5.23,
          "pnlPct": -2.64,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 1
        }
      ]
    },
    "parameters": {
      "obLookback": 8,
      "rsiLongMin": 45,
      "rsiLongMax": 65,
      "volMultiplier": 1.35,
      "takeProfitPct": 3.5,
      "stopLossPct": 1.5,
      "atrMultiplier": 1.5,
      "symbol": "BTC/USDT",
      "timeframe": "15m"
    }
  },
  {
    "id": "STRAT_SMC_LUXALGO_5X_BTC_USDT_15m_103020",
    "title": "LuxAlgo SMC & 5X Futures Leverage Strategy (BTC/USDT 15m)",
    "name": "LuxAlgo SMC & 5X Futures Leverage Strategy (BTC/USDT 15m)",
    "type": "smc_luxalgo_5x",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X",
    "channel_source": "LuxAlgo Official & YouTube Alpha",
    "target_win_rate_pct": 28.6,
    "concepts": [
      "Order Block (OB) Identification",
      "Liquidity Sweep Invalidation",
      "Dynamic ATR Trailing Stop (1.5x)",
      "Isolated 5X Leverage Margin"
    ],
    "risk_management": {
      "take_profit_pct": 4.0,
      "take_profit_leveraged_pct": 20.0,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 0.93
    },
    "pinescript_code": "//@version=5\nstrategy(\"AiTradingAgent \u2014 LuxAlgo SMC & 5X Futures Strategy (BTC/USDT)\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05, process_orders_on_close=true)\n\n// ==============================================================================\n// AiTradingAgent v4 \u2014 Autonomous PineScript v5 Strategy\n// Symbol: BTC/USDT | Leverage: 5X Isolated Margin\n// Target Win Rate: >68.0% | Risk-Reward Ratio: 2.50:1\n// Concepts: LuxAlgo Order Blocks (OB), Liquidity Sweeps, Fair Value Gaps (FVG)\n// ==============================================================================\n\n// \u2500\u2500\u2500 Input Configuration \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(12, \"Order Block Lookback Period\", minval=3, maxval=50, group=grp_smc)\nuse_fvg      = input.bool(true, \"Enable Fair Value Gap (FVG) Confirmation\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity Multiplier\", minval=0.5, step=0.1, group=grp_smc)\n\ngrp_momentum = \"Oscillators & Volume Regime\"\nrsi_len      = input.int(14, \"RSI Calculation Length\", minval=2, group=grp_momentum)\nrsi_long_min = input.int(46, \"RSI Long Minimum (Expansion Gate)\", minval=30, maxval=70, group=grp_momentum)\nrsi_long_max = input.int(70, \"RSI Long Maximum (Overbought Gate)\", minval=50, maxval=90, group=grp_momentum)\nrsi_short_min= input.int(32, \"RSI Short Minimum (Oversold Gate)\", minval=10, maxval=50, group=grp_momentum)\nrsi_short_max= input.int(55, \"RSI Short Maximum (Compression Gate)\", minval=30, maxval=70, group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index (MFI) Length\", minval=5, group=grp_momentum)\nvol_mult     = input.float(1.5, \"Institutional Volume Multiplier\", minval=1.0, step=0.05, group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5, \"Futures Leverage Multiplier (Isolated)\", minval=1.0, maxval=20.0, step=0.5, group=grp_futures)\ntp_pct_raw   = input.float(4.5, \"Underlying Take-Profit %\", minval=0.5, step=0.1, group=grp_futures)\nsl_pct_raw   = input.float(1.8, \"Underlying Stop-Loss %\", minval=0.5, step=0.1, group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", minval=5, group=grp_futures)\natr_sl_mult  = input.float(1.8, \"ATR Trailing Stop Multiplier\", minval=0.5, step=0.1, group=grp_futures)\nuse_trailing = input.bool(true, \"Enable Dynamic ATR Trailing Stop\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\nema_20  = ta.ema(close, 20)\nema_50  = ta.ema(close, 50)\nema_200 = ta.ema(close, 200)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nvol_surge     = volume > (vol_ma * vol_mult)\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and vol_surge\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and vol_surge\n\n// Fair Value Gap (FVG) Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum & Trend Confluence\nbull_momentum = (rsi_v >= rsi_long_min and rsi_v <= rsi_long_max) and (mfi_v >= 50.0)\nbear_momentum = (rsi_v <= rsi_short_max and rsi_v >= rsi_short_min) and (mfi_v <= 50.0)\ntrend_bullish = close > ema_20 and ema_20 > ema_50\ntrend_bearish = close < ema_20 and ema_20 < ema_50\n\n// Entry Trigger Logic\nlong_condition  = (bullish_sweep or (bullish_fvg and trend_bullish)) and bull_momentum and ta.crossover(close, ema_20)\nshort_condition = (bearish_sweep or (bearish_fvg and trend_bearish)) and bear_momentum and ta.crossunder(close, ema_20)\n\n// \u2500\u2500\u2500 Strategy Execution & Position Management \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl_price  = na\nvar float long_tp_price  = na\nvar float short_sl_price = na\nvar float short_tp_price = na\n\nif (long_condition and strategy.position_size == 0)\n    long_sl_price := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp_price := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"LONG\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl_price) + ',\"tp\":' + str.tostring(long_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_LONG\", strategy.long, alert_message=alert_json)\n\nif (short_condition and strategy.position_size == 0)\n    short_sl_price := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp_price := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"SHORT\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl_price) + ',\"tp\":' + str.tostring(short_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_SHORT\", strategy.short, alert_message=alert_json)\n\n// Dynamic ATR Trailing Stops & Limit Exits\nif (strategy.position_size > 0)\n    if (use_trailing)\n        long_sl_price := math.max(nz(long_sl_price, close - (atr_v * atr_sl_mult)), close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"SMC_5X_LONG\", stop=long_sl_price, limit=long_tp_price)\n\nif (strategy.position_size < 0)\n    if (use_trailing)\n        short_sl_price := math.min(nz(short_sl_price, close + (atr_v * atr_sl_mult)), close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SMC_5X_SHORT\", stop=short_sl_price, limit=short_tp_price)\n\n// \u2500\u2500\u2500 Visual Plots & Chart Overlays \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ema_20, \"EMA 20\", color=color.new(#3b82f6, 0), linewidth=1)\nplot(ema_50, \"EMA 50\", color=color.new(#8b5cf6, 0), linewidth=2)\nplot(ema_200, \"EMA 200\", color=color.new(#f59e0b, 0), linewidth=2)\n\nplotshape(long_condition and strategy.position_size == 0, title=\"SMC Long Signal\", location=location.belowbar, color=color.new(#10b981, 0), style=shape.triangleup, size=size.normal, text=\"5X BUY\")\nplotshape(short_condition and strategy.position_size == 0, title=\"SMC Short Signal\", location=location.abovebar, color=color.new(#ef4444, 0), style=shape.triangledown, size=size.normal, text=\"5X SELL\")\nplotshape(bullish_sweep, title=\"Bullish Liquidity Sweep\", location=location.belowbar, color=color.new(#06b6d4, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\nplotshape(bearish_sweep, title=\"Bearish Liquidity Sweep\", location=location.abovebar, color=color.new(#f97316, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\n",
    "pinescript": "//@version=5\nstrategy(\"AiTradingAgent \u2014 LuxAlgo SMC & 5X Futures Strategy (BTC/USDT)\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05, process_orders_on_close=true)\n\n// ==============================================================================\n// AiTradingAgent v4 \u2014 Autonomous PineScript v5 Strategy\n// Symbol: BTC/USDT | Leverage: 5X Isolated Margin\n// Target Win Rate: >68.0% | Risk-Reward Ratio: 2.50:1\n// Concepts: LuxAlgo Order Blocks (OB), Liquidity Sweeps, Fair Value Gaps (FVG)\n// ==============================================================================\n\n// \u2500\u2500\u2500 Input Configuration \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(12, \"Order Block Lookback Period\", minval=3, maxval=50, group=grp_smc)\nuse_fvg      = input.bool(true, \"Enable Fair Value Gap (FVG) Confirmation\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity Multiplier\", minval=0.5, step=0.1, group=grp_smc)\n\ngrp_momentum = \"Oscillators & Volume Regime\"\nrsi_len      = input.int(14, \"RSI Calculation Length\", minval=2, group=grp_momentum)\nrsi_long_min = input.int(46, \"RSI Long Minimum (Expansion Gate)\", minval=30, maxval=70, group=grp_momentum)\nrsi_long_max = input.int(70, \"RSI Long Maximum (Overbought Gate)\", minval=50, maxval=90, group=grp_momentum)\nrsi_short_min= input.int(32, \"RSI Short Minimum (Oversold Gate)\", minval=10, maxval=50, group=grp_momentum)\nrsi_short_max= input.int(55, \"RSI Short Maximum (Compression Gate)\", minval=30, maxval=70, group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index (MFI) Length\", minval=5, group=grp_momentum)\nvol_mult     = input.float(1.5, \"Institutional Volume Multiplier\", minval=1.0, step=0.05, group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5, \"Futures Leverage Multiplier (Isolated)\", minval=1.0, maxval=20.0, step=0.5, group=grp_futures)\ntp_pct_raw   = input.float(4.5, \"Underlying Take-Profit %\", minval=0.5, step=0.1, group=grp_futures)\nsl_pct_raw   = input.float(1.8, \"Underlying Stop-Loss %\", minval=0.5, step=0.1, group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", minval=5, group=grp_futures)\natr_sl_mult  = input.float(1.8, \"ATR Trailing Stop Multiplier\", minval=0.5, step=0.1, group=grp_futures)\nuse_trailing = input.bool(true, \"Enable Dynamic ATR Trailing Stop\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\nema_20  = ta.ema(close, 20)\nema_50  = ta.ema(close, 50)\nema_200 = ta.ema(close, 200)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nvol_surge     = volume > (vol_ma * vol_mult)\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and vol_surge\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and vol_surge\n\n// Fair Value Gap (FVG) Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum & Trend Confluence\nbull_momentum = (rsi_v >= rsi_long_min and rsi_v <= rsi_long_max) and (mfi_v >= 50.0)\nbear_momentum = (rsi_v <= rsi_short_max and rsi_v >= rsi_short_min) and (mfi_v <= 50.0)\ntrend_bullish = close > ema_20 and ema_20 > ema_50\ntrend_bearish = close < ema_20 and ema_20 < ema_50\n\n// Entry Trigger Logic\nlong_condition  = (bullish_sweep or (bullish_fvg and trend_bullish)) and bull_momentum and ta.crossover(close, ema_20)\nshort_condition = (bearish_sweep or (bearish_fvg and trend_bearish)) and bear_momentum and ta.crossunder(close, ema_20)\n\n// \u2500\u2500\u2500 Strategy Execution & Position Management \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl_price  = na\nvar float long_tp_price  = na\nvar float short_sl_price = na\nvar float short_tp_price = na\n\nif (long_condition and strategy.position_size == 0)\n    long_sl_price := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp_price := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"LONG\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl_price) + ',\"tp\":' + str.tostring(long_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_LONG\", strategy.long, alert_message=alert_json)\n\nif (short_condition and strategy.position_size == 0)\n    short_sl_price := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp_price := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"SHORT\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl_price) + ',\"tp\":' + str.tostring(short_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_SHORT\", strategy.short, alert_message=alert_json)\n\n// Dynamic ATR Trailing Stops & Limit Exits\nif (strategy.position_size > 0)\n    if (use_trailing)\n        long_sl_price := math.max(nz(long_sl_price, close - (atr_v * atr_sl_mult)), close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"SMC_5X_LONG\", stop=long_sl_price, limit=long_tp_price)\n\nif (strategy.position_size < 0)\n    if (use_trailing)\n        short_sl_price := math.min(nz(short_sl_price, close + (atr_v * atr_sl_mult)), close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SMC_5X_SHORT\", stop=short_sl_price, limit=short_tp_price)\n\n// \u2500\u2500\u2500 Visual Plots & Chart Overlays \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ema_20, \"EMA 20\", color=color.new(#3b82f6, 0), linewidth=1)\nplot(ema_50, \"EMA 50\", color=color.new(#8b5cf6, 0), linewidth=2)\nplot(ema_200, \"EMA 200\", color=color.new(#f59e0b, 0), linewidth=2)\n\nplotshape(long_condition and strategy.position_size == 0, title=\"SMC Long Signal\", location=location.belowbar, color=color.new(#10b981, 0), style=shape.triangleup, size=size.normal, text=\"5X BUY\")\nplotshape(short_condition and strategy.position_size == 0, title=\"SMC Short Signal\", location=location.abovebar, color=color.new(#ef4444, 0), style=shape.triangledown, size=size.normal, text=\"5X SELL\")\nplotshape(bullish_sweep, title=\"Bullish Liquidity Sweep\", location=location.belowbar, color=color.new(#06b6d4, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\nplotshape(bearish_sweep, title=\"Bearish Liquidity Sweep\", location=location.abovebar, color=color.new(#f97316, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\n",
    "learned_at": "2026-08-31T15:11:43.020Z",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {
      "strategyType": "smc_luxalgo_5x",
      "symbol": "BTC/USDT",
      "timeframe": "15m",
      "leverage": "5X",
      "initialCapital": 1000,
      "finalCapital": 982.72,
      "netProfitUsd": -17.28,
      "netProfitPct": -1.73,
      "totalTrades": 7,
      "winCount": 2,
      "lossCount": 5,
      "winRate": 28.6,
      "profitFactor": 0.373,
      "maxDrawdownUsd": 19.09,
      "maxDrawdownPct": 1.91,
      "sharpeRatio": -8.1,
      "sortinoRatio": -8.12,
      "riskRewardRatio": 0.93,
      "kellyOptimalFractionPct": 0,
      "gate68Met": false,
      "parameters": {
        "obLookback": 12,
        "rsiLongMin": 46,
        "rsiLongMax": 70,
        "volMultiplier": 1.5,
        "takeProfitPct": 4.5,
        "stopLossPct": 1.8,
        "atrMultiplier": 1.8,
        "symbol": "BTC/USDT",
        "timeframe": "15m"
      },
      "trades": [
        {
          "tradeNum": 1,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-29T02:26:43.003Z",
          "exitTime": "2026-08-29T05:41:43.003Z",
          "entryPrice": 77425.0019,
          "exitPrice": 77026.6259,
          "collateralUsd": 200,
          "positionSizeUsd": 1000,
          "leverage": 5,
          "pnlUsd": -6.65,
          "pnlPct": -3.32,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 13
        },
        {
          "tradeNum": 2,
          "symbol": "BTC/USDT",
          "side": "SELL",
          "entryTime": "2026-08-29T10:11:43.003Z",
          "exitTime": "2026-08-29T11:26:43.003Z",
          "entryPrice": 77092.7156,
          "exitPrice": 77494.617,
          "collateralUsd": 198.67,
          "positionSizeUsd": 993.35,
          "leverage": 5,
          "pnlUsd": -6.67,
          "pnlPct": -3.36,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 5
        },
        {
          "tradeNum": 3,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-29T16:26:43.003Z",
          "exitTime": "2026-08-30T07:41:43.003Z",
          "entryPrice": 77502.8933,
          "exitPrice": 78283.8603,
          "collateralUsd": 197.34,
          "positionSizeUsd": 986.69,
          "leverage": 5,
          "pnlUsd": 8.46,
          "pnlPct": 4.29,
          "exitReason": "STOP_LOSS",
          "isWin": true,
          "durationBars": 61
        },
        {
          "tradeNum": 4,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-30T12:41:43.003Z",
          "exitTime": "2026-08-30T21:56:43.003Z",
          "entryPrice": 78296.22,
          "exitPrice": 78249.1801,
          "collateralUsd": 199.03,
          "positionSizeUsd": 995.15,
          "leverage": 5,
          "pnlUsd": -2.09,
          "pnlPct": -1.05,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 37
        },
        {
          "tradeNum": 5,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-30T23:26:43.003Z",
          "exitTime": "2026-08-31T00:26:43.003Z",
          "entryPrice": 78449.1366,
          "exitPrice": 78097.0386,
          "collateralUsd": 198.61,
          "positionSizeUsd": 993.06,
          "leverage": 5,
          "pnlUsd": -5.95,
          "pnlPct": -2.99,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 4
        },
        {
          "tradeNum": 6,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-31T03:26:43.003Z",
          "exitTime": "2026-08-31T04:56:43.003Z",
          "entryPrice": 78461.6808,
          "exitPrice": 78086.0928,
          "collateralUsd": 197.42,
          "positionSizeUsd": 987.11,
          "leverage": 5,
          "pnlUsd": -6.21,
          "pnlPct": -3.14,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 6
        },
        {
          "tradeNum": 7,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-31T10:41:43.003Z",
          "exitTime": "2026-08-31T14:56:43.003Z",
          "entryPrice": 78505.7347,
          "exitPrice": 78769.1055,
          "collateralUsd": 196.18,
          "positionSizeUsd": 980.91,
          "leverage": 5,
          "pnlUsd": 1.82,
          "pnlPct": 0.93,
          "exitReason": "END_OF_DATA",
          "isWin": true,
          "durationBars": 17
        }
      ]
    },
    "parameters": {
      "obLookback": 12,
      "rsiLongMin": 46,
      "rsiLongMax": 70,
      "volMultiplier": 1.5,
      "takeProfitPct": 4.5,
      "stopLossPct": 1.8,
      "atrMultiplier": 1.8,
      "symbol": "BTC/USDT",
      "timeframe": "15m"
    }
  },
  {
    "id": "STRAT_SMC_LUXALGO_5X_BTC_USDT_15m_533844",
    "title": "LuxAlgo SMC & 5X Futures Leverage Strategy (BTC/USDT 15m)",
    "name": "LuxAlgo SMC & 5X Futures Leverage Strategy (BTC/USDT 15m)",
    "type": "smc_luxalgo_5x",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X",
    "channel_source": "LuxAlgo Official & YouTube Alpha",
    "target_win_rate_pct": 0,
    "concepts": [
      "Order Block (OB) Identification",
      "Liquidity Sweep Invalidation",
      "Dynamic ATR Trailing Stop (1.5x)",
      "Isolated 5X Leverage Margin"
    ],
    "risk_management": {
      "take_profit_pct": 4.0,
      "take_profit_leveraged_pct": 20.0,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.5
    },
    "pinescript_code": "//@version=5\nstrategy(\"AiTradingAgent \u2014 LuxAlgo SMC & 5X Futures Strategy (BTC/USDT)\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05, process_orders_on_close=true)\n\n// ==============================================================================\n// AiTradingAgent v4 \u2014 Autonomous PineScript v5 Strategy\n// Symbol: BTC/USDT | Leverage: 5X Isolated Margin\n// Target Win Rate: >68.0% | Risk-Reward Ratio: 2.33:1\n// Concepts: LuxAlgo Order Blocks (OB), Liquidity Sweeps, Fair Value Gaps (FVG)\n// ==============================================================================\n\n// \u2500\u2500\u2500 Input Configuration \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(8, \"Order Block Lookback Period\", minval=3, maxval=50, group=grp_smc)\nuse_fvg      = input.bool(true, \"Enable Fair Value Gap (FVG) Confirmation\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity Multiplier\", minval=0.5, step=0.1, group=grp_smc)\n\ngrp_momentum = \"Oscillators & Volume Regime\"\nrsi_len      = input.int(14, \"RSI Calculation Length\", minval=2, group=grp_momentum)\nrsi_long_min = input.int(45, \"RSI Long Minimum (Expansion Gate)\", minval=30, maxval=70, group=grp_momentum)\nrsi_long_max = input.int(65, \"RSI Long Maximum (Overbought Gate)\", minval=50, maxval=90, group=grp_momentum)\nrsi_short_min= input.int(32, \"RSI Short Minimum (Oversold Gate)\", minval=10, maxval=50, group=grp_momentum)\nrsi_short_max= input.int(55, \"RSI Short Maximum (Compression Gate)\", minval=30, maxval=70, group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index (MFI) Length\", minval=5, group=grp_momentum)\nvol_mult     = input.float(1.35, \"Institutional Volume Multiplier\", minval=1.0, step=0.05, group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5, \"Futures Leverage Multiplier (Isolated)\", minval=1.0, maxval=20.0, step=0.5, group=grp_futures)\ntp_pct_raw   = input.float(3.5, \"Underlying Take-Profit %\", minval=0.5, step=0.1, group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Underlying Stop-Loss %\", minval=0.5, step=0.1, group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", minval=5, group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Stop Multiplier\", minval=0.5, step=0.1, group=grp_futures)\nuse_trailing = input.bool(true, \"Enable Dynamic ATR Trailing Stop\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\nema_20  = ta.ema(close, 20)\nema_50  = ta.ema(close, 50)\nema_200 = ta.ema(close, 200)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nvol_surge     = volume > (vol_ma * vol_mult)\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and vol_surge\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and vol_surge\n\n// Fair Value Gap (FVG) Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum & Trend Confluence\nbull_momentum = (rsi_v >= rsi_long_min and rsi_v <= rsi_long_max) and (mfi_v >= 50.0)\nbear_momentum = (rsi_v <= rsi_short_max and rsi_v >= rsi_short_min) and (mfi_v <= 50.0)\ntrend_bullish = close > ema_20 and ema_20 > ema_50\ntrend_bearish = close < ema_20 and ema_20 < ema_50\n\n// Entry Trigger Logic\nlong_condition  = (bullish_sweep or (bullish_fvg and trend_bullish)) and bull_momentum and ta.crossover(close, ema_20)\nshort_condition = (bearish_sweep or (bearish_fvg and trend_bearish)) and bear_momentum and ta.crossunder(close, ema_20)\n\n// \u2500\u2500\u2500 Strategy Execution & Position Management \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl_price  = na\nvar float long_tp_price  = na\nvar float short_sl_price = na\nvar float short_tp_price = na\n\nif (long_condition and strategy.position_size == 0)\n    long_sl_price := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp_price := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"LONG\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl_price) + ',\"tp\":' + str.tostring(long_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_LONG\", strategy.long, alert_message=alert_json)\n\nif (short_condition and strategy.position_size == 0)\n    short_sl_price := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp_price := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"SHORT\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl_price) + ',\"tp\":' + str.tostring(short_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_SHORT\", strategy.short, alert_message=alert_json)\n\n// Dynamic ATR Trailing Stops & Limit Exits\nif (strategy.position_size > 0)\n    if (use_trailing)\n        long_sl_price := math.max(nz(long_sl_price, close - (atr_v * atr_sl_mult)), close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"SMC_5X_LONG\", stop=long_sl_price, limit=long_tp_price)\n\nif (strategy.position_size < 0)\n    if (use_trailing)\n        short_sl_price := math.min(nz(short_sl_price, close + (atr_v * atr_sl_mult)), close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SMC_5X_SHORT\", stop=short_sl_price, limit=short_tp_price)\n\n// \u2500\u2500\u2500 Visual Plots & Chart Overlays \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ema_20, \"EMA 20\", color=color.new(#3b82f6, 0), linewidth=1)\nplot(ema_50, \"EMA 50\", color=color.new(#8b5cf6, 0), linewidth=2)\nplot(ema_200, \"EMA 200\", color=color.new(#f59e0b, 0), linewidth=2)\n\nplotshape(long_condition and strategy.position_size == 0, title=\"SMC Long Signal\", location=location.belowbar, color=color.new(#10b981, 0), style=shape.triangleup, size=size.normal, text=\"5X BUY\")\nplotshape(short_condition and strategy.position_size == 0, title=\"SMC Short Signal\", location=location.abovebar, color=color.new(#ef4444, 0), style=shape.triangledown, size=size.normal, text=\"5X SELL\")\nplotshape(bullish_sweep, title=\"Bullish Liquidity Sweep\", location=location.belowbar, color=color.new(#06b6d4, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\nplotshape(bearish_sweep, title=\"Bearish Liquidity Sweep\", location=location.abovebar, color=color.new(#f97316, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\n",
    "pinescript": "//@version=5\nstrategy(\"AiTradingAgent \u2014 LuxAlgo SMC & 5X Futures Strategy (BTC/USDT)\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05, process_orders_on_close=true)\n\n// ==============================================================================\n// AiTradingAgent v4 \u2014 Autonomous PineScript v5 Strategy\n// Symbol: BTC/USDT | Leverage: 5X Isolated Margin\n// Target Win Rate: >68.0% | Risk-Reward Ratio: 2.33:1\n// Concepts: LuxAlgo Order Blocks (OB), Liquidity Sweeps, Fair Value Gaps (FVG)\n// ==============================================================================\n\n// \u2500\u2500\u2500 Input Configuration \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(8, \"Order Block Lookback Period\", minval=3, maxval=50, group=grp_smc)\nuse_fvg      = input.bool(true, \"Enable Fair Value Gap (FVG) Confirmation\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity Multiplier\", minval=0.5, step=0.1, group=grp_smc)\n\ngrp_momentum = \"Oscillators & Volume Regime\"\nrsi_len      = input.int(14, \"RSI Calculation Length\", minval=2, group=grp_momentum)\nrsi_long_min = input.int(45, \"RSI Long Minimum (Expansion Gate)\", minval=30, maxval=70, group=grp_momentum)\nrsi_long_max = input.int(65, \"RSI Long Maximum (Overbought Gate)\", minval=50, maxval=90, group=grp_momentum)\nrsi_short_min= input.int(32, \"RSI Short Minimum (Oversold Gate)\", minval=10, maxval=50, group=grp_momentum)\nrsi_short_max= input.int(55, \"RSI Short Maximum (Compression Gate)\", minval=30, maxval=70, group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index (MFI) Length\", minval=5, group=grp_momentum)\nvol_mult     = input.float(1.35, \"Institutional Volume Multiplier\", minval=1.0, step=0.05, group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5, \"Futures Leverage Multiplier (Isolated)\", minval=1.0, maxval=20.0, step=0.5, group=grp_futures)\ntp_pct_raw   = input.float(3.5, \"Underlying Take-Profit %\", minval=0.5, step=0.1, group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Underlying Stop-Loss %\", minval=0.5, step=0.1, group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", minval=5, group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Stop Multiplier\", minval=0.5, step=0.1, group=grp_futures)\nuse_trailing = input.bool(true, \"Enable Dynamic ATR Trailing Stop\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\nema_20  = ta.ema(close, 20)\nema_50  = ta.ema(close, 50)\nema_200 = ta.ema(close, 200)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nvol_surge     = volume > (vol_ma * vol_mult)\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and vol_surge\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and vol_surge\n\n// Fair Value Gap (FVG) Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum & Trend Confluence\nbull_momentum = (rsi_v >= rsi_long_min and rsi_v <= rsi_long_max) and (mfi_v >= 50.0)\nbear_momentum = (rsi_v <= rsi_short_max and rsi_v >= rsi_short_min) and (mfi_v <= 50.0)\ntrend_bullish = close > ema_20 and ema_20 > ema_50\ntrend_bearish = close < ema_20 and ema_20 < ema_50\n\n// Entry Trigger Logic\nlong_condition  = (bullish_sweep or (bullish_fvg and trend_bullish)) and bull_momentum and ta.crossover(close, ema_20)\nshort_condition = (bearish_sweep or (bearish_fvg and trend_bearish)) and bear_momentum and ta.crossunder(close, ema_20)\n\n// \u2500\u2500\u2500 Strategy Execution & Position Management \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl_price  = na\nvar float long_tp_price  = na\nvar float short_sl_price = na\nvar float short_tp_price = na\n\nif (long_condition and strategy.position_size == 0)\n    long_sl_price := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp_price := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"LONG\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl_price) + ',\"tp\":' + str.tostring(long_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_LONG\", strategy.long, alert_message=alert_json)\n\nif (short_condition and strategy.position_size == 0)\n    short_sl_price := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp_price := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"SHORT\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl_price) + ',\"tp\":' + str.tostring(short_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_SHORT\", strategy.short, alert_message=alert_json)\n\n// Dynamic ATR Trailing Stops & Limit Exits\nif (strategy.position_size > 0)\n    if (use_trailing)\n        long_sl_price := math.max(nz(long_sl_price, close - (atr_v * atr_sl_mult)), close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"SMC_5X_LONG\", stop=long_sl_price, limit=long_tp_price)\n\nif (strategy.position_size < 0)\n    if (use_trailing)\n        short_sl_price := math.min(nz(short_sl_price, close + (atr_v * atr_sl_mult)), close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SMC_5X_SHORT\", stop=short_sl_price, limit=short_tp_price)\n\n// \u2500\u2500\u2500 Visual Plots & Chart Overlays \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ema_20, \"EMA 20\", color=color.new(#3b82f6, 0), linewidth=1)\nplot(ema_50, \"EMA 50\", color=color.new(#8b5cf6, 0), linewidth=2)\nplot(ema_200, \"EMA 200\", color=color.new(#f59e0b, 0), linewidth=2)\n\nplotshape(long_condition and strategy.position_size == 0, title=\"SMC Long Signal\", location=location.belowbar, color=color.new(#10b981, 0), style=shape.triangleup, size=size.normal, text=\"5X BUY\")\nplotshape(short_condition and strategy.position_size == 0, title=\"SMC Short Signal\", location=location.abovebar, color=color.new(#ef4444, 0), style=shape.triangledown, size=size.normal, text=\"5X SELL\")\nplotshape(bullish_sweep, title=\"Bullish Liquidity Sweep\", location=location.belowbar, color=color.new(#06b6d4, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\nplotshape(bearish_sweep, title=\"Bearish Liquidity Sweep\", location=location.abovebar, color=color.new(#f97316, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\n",
    "learned_at": "2026-08-31T15:02:13.844Z",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {
      "strategyType": "smc_luxalgo_5x",
      "symbol": "BTC/USDT",
      "timeframe": "15m",
      "leverage": "5X",
      "initialCapital": 1000,
      "finalCapital": 986.41,
      "netProfitUsd": -13.59,
      "netProfitPct": -1.36,
      "totalTrades": 5,
      "winCount": 0,
      "lossCount": 5,
      "winRate": 0,
      "profitFactor": 0,
      "maxDrawdownUsd": 13.59,
      "maxDrawdownPct": 1.36,
      "sharpeRatio": -24.7,
      "sortinoRatio": -15.71,
      "riskRewardRatio": 0,
      "kellyOptimalFractionPct": 0,
      "gate68Met": false,
      "parameters": {
        "obLookback": 8,
        "rsiLongMin": 45,
        "rsiLongMax": 65,
        "volMultiplier": 1.35,
        "takeProfitPct": 3.5,
        "stopLossPct": 1.5,
        "atrMultiplier": 1.5,
        "symbol": "BTC/USDT",
        "timeframe": "15m"
      },
      "trades": [
        {
          "tradeNum": 1,
          "symbol": "BTC/USDT",
          "side": "SELL",
          "entryTime": "2026-08-29T05:15:00.000Z",
          "exitTime": "2026-08-29T06:00:00.000Z",
          "entryPrice": 77660.01,
          "exitPrice": 77608.1,
          "collateralUsd": 200,
          "positionSizeUsd": 1000,
          "leverage": 5,
          "pnlUsd": -0.83,
          "pnlPct": -0.42,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 3
        },
        {
          "tradeNum": 2,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-29T21:45:00.000Z",
          "exitTime": "2026-08-30T01:00:00.000Z",
          "entryPrice": 78161.63,
          "exitPrice": 78146.485,
          "collateralUsd": 199.83,
          "positionSizeUsd": 999.17,
          "leverage": 5,
          "pnlUsd": -1.69,
          "pnlPct": -0.85,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 13
        },
        {
          "tradeNum": 3,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-30T05:00:00.000Z",
          "exitTime": "2026-08-30T07:30:00.000Z",
          "entryPrice": 78146.01,
          "exitPrice": 78176.945,
          "collateralUsd": 199.5,
          "positionSizeUsd": 997.48,
          "leverage": 5,
          "pnlUsd": -1.1,
          "pnlPct": -0.55,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 10
        },
        {
          "tradeNum": 4,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-30T18:15:00.000Z",
          "exitTime": "2026-08-30T19:45:00.000Z",
          "entryPrice": 79009.61,
          "exitPrice": 78752.785,
          "collateralUsd": 199.27,
          "positionSizeUsd": 996.37,
          "leverage": 5,
          "pnlUsd": -4.73,
          "pnlPct": -2.38,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 6
        },
        {
          "tradeNum": 5,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-31T12:00:00.000Z",
          "exitTime": "2026-08-31T12:15:00.000Z",
          "entryPrice": 78410.01,
          "exitPrice": 78114.075,
          "collateralUsd": 198.33,
          "positionSizeUsd": 991.64,
          "leverage": 5,
          "pnlUsd": -5.23,
          "pnlPct": -2.64,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 1
        }
      ]
    },
    "parameters": {
      "obLookback": 8,
      "rsiLongMin": 45,
      "rsiLongMax": 65,
      "volMultiplier": 1.35,
      "takeProfitPct": 3.5,
      "stopLossPct": 1.5,
      "atrMultiplier": 1.5,
      "symbol": "BTC/USDT",
      "timeframe": "15m"
    }
  },
  {
    "id": "STRAT_SMC_LUXALGO_5X_BTC_USDT_15m_426025",
    "title": "LuxAlgo SMC & 5X Futures Leverage Strategy (BTC/USDT 15m)",
    "name": "LuxAlgo SMC & 5X Futures Leverage Strategy (BTC/USDT 15m)",
    "type": "smc_luxalgo_5x",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X",
    "channel_source": "LuxAlgo Official & YouTube Alpha",
    "target_win_rate_pct": 12.5,
    "concepts": [
      "Order Block (OB) Identification",
      "Liquidity Sweep Invalidation",
      "Dynamic ATR Trailing Stop (1.5x)",
      "Isolated 5X Leverage Margin"
    ],
    "risk_management": {
      "take_profit_pct": 4.0,
      "take_profit_leveraged_pct": 20.0,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 0.62
    },
    "pinescript_code": "//@version=5\nstrategy(\"AiTradingAgent \u2014 LuxAlgo SMC & 5X Futures Strategy (BTC/USDT)\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05, process_orders_on_close=true)\n\n// ==============================================================================\n// AiTradingAgent v4 \u2014 Autonomous PineScript v5 Strategy\n// Symbol: BTC/USDT | Leverage: 5X Isolated Margin\n// Target Win Rate: >68.0% | Risk-Reward Ratio: 2.77:1\n// Concepts: LuxAlgo Order Blocks (OB), Liquidity Sweeps, Fair Value Gaps (FVG)\n// ==============================================================================\n\n// \u2500\u2500\u2500 Input Configuration \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(6, \"Order Block Lookback Period\", minval=3, maxval=50, group=grp_smc)\nuse_fvg      = input.bool(true, \"Enable Fair Value Gap (FVG) Confirmation\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity Multiplier\", minval=0.5, step=0.1, group=grp_smc)\n\ngrp_momentum = \"Oscillators & Volume Regime\"\nrsi_len      = input.int(14, \"RSI Calculation Length\", minval=2, group=grp_momentum)\nrsi_long_min = input.int(44, \"RSI Long Minimum (Expansion Gate)\", minval=30, maxval=70, group=grp_momentum)\nrsi_long_max = input.int(66, \"RSI Long Maximum (Overbought Gate)\", minval=50, maxval=90, group=grp_momentum)\nrsi_short_min= input.int(32, \"RSI Short Minimum (Oversold Gate)\", minval=10, maxval=50, group=grp_momentum)\nrsi_short_max= input.int(55, \"RSI Short Maximum (Compression Gate)\", minval=30, maxval=70, group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index (MFI) Length\", minval=5, group=grp_momentum)\nvol_mult     = input.float(1.4, \"Institutional Volume Multiplier\", minval=1.0, step=0.05, group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5, \"Futures Leverage Multiplier (Isolated)\", minval=1.0, maxval=20.0, step=0.5, group=grp_futures)\ntp_pct_raw   = input.float(3.6, \"Underlying Take-Profit %\", minval=0.5, step=0.1, group=grp_futures)\nsl_pct_raw   = input.float(1.3, \"Underlying Stop-Loss %\", minval=0.5, step=0.1, group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", minval=5, group=grp_futures)\natr_sl_mult  = input.float(1.3, \"ATR Trailing Stop Multiplier\", minval=0.5, step=0.1, group=grp_futures)\nuse_trailing = input.bool(true, \"Enable Dynamic ATR Trailing Stop\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\nema_20  = ta.ema(close, 20)\nema_50  = ta.ema(close, 50)\nema_200 = ta.ema(close, 200)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nvol_surge     = volume > (vol_ma * vol_mult)\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and vol_surge\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and vol_surge\n\n// Fair Value Gap (FVG) Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum & Trend Confluence\nbull_momentum = (rsi_v >= rsi_long_min and rsi_v <= rsi_long_max) and (mfi_v >= 50.0)\nbear_momentum = (rsi_v <= rsi_short_max and rsi_v >= rsi_short_min) and (mfi_v <= 50.0)\ntrend_bullish = close > ema_20 and ema_20 > ema_50\ntrend_bearish = close < ema_20 and ema_20 < ema_50\n\n// Entry Trigger Logic\nlong_condition  = (bullish_sweep or (bullish_fvg and trend_bullish)) and bull_momentum and ta.crossover(close, ema_20)\nshort_condition = (bearish_sweep or (bearish_fvg and trend_bearish)) and bear_momentum and ta.crossunder(close, ema_20)\n\n// \u2500\u2500\u2500 Strategy Execution & Position Management \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl_price  = na\nvar float long_tp_price  = na\nvar float short_sl_price = na\nvar float short_tp_price = na\n\nif (long_condition and strategy.position_size == 0)\n    long_sl_price := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp_price := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"LONG\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl_price) + ',\"tp\":' + str.tostring(long_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_LONG\", strategy.long, alert_message=alert_json)\n\nif (short_condition and strategy.position_size == 0)\n    short_sl_price := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp_price := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"SHORT\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl_price) + ',\"tp\":' + str.tostring(short_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_SHORT\", strategy.short, alert_message=alert_json)\n\n// Dynamic ATR Trailing Stops & Limit Exits\nif (strategy.position_size > 0)\n    if (use_trailing)\n        long_sl_price := math.max(nz(long_sl_price, close - (atr_v * atr_sl_mult)), close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"SMC_5X_LONG\", stop=long_sl_price, limit=long_tp_price)\n\nif (strategy.position_size < 0)\n    if (use_trailing)\n        short_sl_price := math.min(nz(short_sl_price, close + (atr_v * atr_sl_mult)), close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SMC_5X_SHORT\", stop=short_sl_price, limit=short_tp_price)\n\n// \u2500\u2500\u2500 Visual Plots & Chart Overlays \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ema_20, \"EMA 20\", color=color.new(#3b82f6, 0), linewidth=1)\nplot(ema_50, \"EMA 50\", color=color.new(#8b5cf6, 0), linewidth=2)\nplot(ema_200, \"EMA 200\", color=color.new(#f59e0b, 0), linewidth=2)\n\nplotshape(long_condition and strategy.position_size == 0, title=\"SMC Long Signal\", location=location.belowbar, color=color.new(#10b981, 0), style=shape.triangleup, size=size.normal, text=\"5X BUY\")\nplotshape(short_condition and strategy.position_size == 0, title=\"SMC Short Signal\", location=location.abovebar, color=color.new(#ef4444, 0), style=shape.triangledown, size=size.normal, text=\"5X SELL\")\nplotshape(bullish_sweep, title=\"Bullish Liquidity Sweep\", location=location.belowbar, color=color.new(#06b6d4, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\nplotshape(bearish_sweep, title=\"Bearish Liquidity Sweep\", location=location.abovebar, color=color.new(#f97316, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\n",
    "pinescript": "//@version=5\nstrategy(\"AiTradingAgent \u2014 LuxAlgo SMC & 5X Futures Strategy (BTC/USDT)\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05, process_orders_on_close=true)\n\n// ==============================================================================\n// AiTradingAgent v4 \u2014 Autonomous PineScript v5 Strategy\n// Symbol: BTC/USDT | Leverage: 5X Isolated Margin\n// Target Win Rate: >68.0% | Risk-Reward Ratio: 2.77:1\n// Concepts: LuxAlgo Order Blocks (OB), Liquidity Sweeps, Fair Value Gaps (FVG)\n// ==============================================================================\n\n// \u2500\u2500\u2500 Input Configuration \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(6, \"Order Block Lookback Period\", minval=3, maxval=50, group=grp_smc)\nuse_fvg      = input.bool(true, \"Enable Fair Value Gap (FVG) Confirmation\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity Multiplier\", minval=0.5, step=0.1, group=grp_smc)\n\ngrp_momentum = \"Oscillators & Volume Regime\"\nrsi_len      = input.int(14, \"RSI Calculation Length\", minval=2, group=grp_momentum)\nrsi_long_min = input.int(44, \"RSI Long Minimum (Expansion Gate)\", minval=30, maxval=70, group=grp_momentum)\nrsi_long_max = input.int(66, \"RSI Long Maximum (Overbought Gate)\", minval=50, maxval=90, group=grp_momentum)\nrsi_short_min= input.int(32, \"RSI Short Minimum (Oversold Gate)\", minval=10, maxval=50, group=grp_momentum)\nrsi_short_max= input.int(55, \"RSI Short Maximum (Compression Gate)\", minval=30, maxval=70, group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index (MFI) Length\", minval=5, group=grp_momentum)\nvol_mult     = input.float(1.4, \"Institutional Volume Multiplier\", minval=1.0, step=0.05, group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5, \"Futures Leverage Multiplier (Isolated)\", minval=1.0, maxval=20.0, step=0.5, group=grp_futures)\ntp_pct_raw   = input.float(3.6, \"Underlying Take-Profit %\", minval=0.5, step=0.1, group=grp_futures)\nsl_pct_raw   = input.float(1.3, \"Underlying Stop-Loss %\", minval=0.5, step=0.1, group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", minval=5, group=grp_futures)\natr_sl_mult  = input.float(1.3, \"ATR Trailing Stop Multiplier\", minval=0.5, step=0.1, group=grp_futures)\nuse_trailing = input.bool(true, \"Enable Dynamic ATR Trailing Stop\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\nema_20  = ta.ema(close, 20)\nema_50  = ta.ema(close, 50)\nema_200 = ta.ema(close, 200)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nvol_surge     = volume > (vol_ma * vol_mult)\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and vol_surge\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and vol_surge\n\n// Fair Value Gap (FVG) Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum & Trend Confluence\nbull_momentum = (rsi_v >= rsi_long_min and rsi_v <= rsi_long_max) and (mfi_v >= 50.0)\nbear_momentum = (rsi_v <= rsi_short_max and rsi_v >= rsi_short_min) and (mfi_v <= 50.0)\ntrend_bullish = close > ema_20 and ema_20 > ema_50\ntrend_bearish = close < ema_20 and ema_20 < ema_50\n\n// Entry Trigger Logic\nlong_condition  = (bullish_sweep or (bullish_fvg and trend_bullish)) and bull_momentum and ta.crossover(close, ema_20)\nshort_condition = (bearish_sweep or (bearish_fvg and trend_bearish)) and bear_momentum and ta.crossunder(close, ema_20)\n\n// \u2500\u2500\u2500 Strategy Execution & Position Management \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl_price  = na\nvar float long_tp_price  = na\nvar float short_sl_price = na\nvar float short_tp_price = na\n\nif (long_condition and strategy.position_size == 0)\n    long_sl_price := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp_price := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"LONG\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl_price) + ',\"tp\":' + str.tostring(long_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_LONG\", strategy.long, alert_message=alert_json)\n\nif (short_condition and strategy.position_size == 0)\n    short_sl_price := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp_price := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"SHORT\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl_price) + ',\"tp\":' + str.tostring(short_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_SHORT\", strategy.short, alert_message=alert_json)\n\n// Dynamic ATR Trailing Stops & Limit Exits\nif (strategy.position_size > 0)\n    if (use_trailing)\n        long_sl_price := math.max(nz(long_sl_price, close - (atr_v * atr_sl_mult)), close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"SMC_5X_LONG\", stop=long_sl_price, limit=long_tp_price)\n\nif (strategy.position_size < 0)\n    if (use_trailing)\n        short_sl_price := math.min(nz(short_sl_price, close + (atr_v * atr_sl_mult)), close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SMC_5X_SHORT\", stop=short_sl_price, limit=short_tp_price)\n\n// \u2500\u2500\u2500 Visual Plots & Chart Overlays \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ema_20, \"EMA 20\", color=color.new(#3b82f6, 0), linewidth=1)\nplot(ema_50, \"EMA 50\", color=color.new(#8b5cf6, 0), linewidth=2)\nplot(ema_200, \"EMA 200\", color=color.new(#f59e0b, 0), linewidth=2)\n\nplotshape(long_condition and strategy.position_size == 0, title=\"SMC Long Signal\", location=location.belowbar, color=color.new(#10b981, 0), style=shape.triangleup, size=size.normal, text=\"5X BUY\")\nplotshape(short_condition and strategy.position_size == 0, title=\"SMC Short Signal\", location=location.abovebar, color=color.new(#ef4444, 0), style=shape.triangledown, size=size.normal, text=\"5X SELL\")\nplotshape(bullish_sweep, title=\"Bullish Liquidity Sweep\", location=location.belowbar, color=color.new(#06b6d4, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\nplotshape(bearish_sweep, title=\"Bearish Liquidity Sweep\", location=location.abovebar, color=color.new(#f97316, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\n",
    "learned_at": "2026-08-31T15:00:26.025Z",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {
      "strategyType": "smc_luxalgo_5x",
      "symbol": "BTC/USDT",
      "timeframe": "15m",
      "leverage": "5X",
      "initialCapital": 1000,
      "finalCapital": 986.41,
      "netProfitUsd": -13.59,
      "netProfitPct": -1.36,
      "totalTrades": 8,
      "winCount": 1,
      "lossCount": 7,
      "winRate": 12.5,
      "profitFactor": 0.089,
      "maxDrawdownUsd": 14.3,
      "maxDrawdownPct": 1.43,
      "sharpeRatio": -15.87,
      "sortinoRatio": -12.07,
      "riskRewardRatio": 0.62,
      "kellyOptimalFractionPct": 0,
      "gate68Met": false,
      "parameters": {
        "obLookback": 6,
        "rsiLongMin": 44,
        "rsiLongMax": 66,
        "volMultiplier": 1.4,
        "takeProfitPct": 3.6,
        "stopLossPct": 1.3,
        "atrMultiplier": 1.3,
        "symbol": "BTC/USDT",
        "timeframe": "15m"
      },
      "trades": [
        {
          "tradeNum": 1,
          "symbol": "BTC/USDT",
          "side": "SELL",
          "entryTime": "2026-08-29T05:15:00.000Z",
          "exitTime": "2026-08-29T06:00:00.000Z",
          "entryPrice": 77660.01,
          "exitPrice": 77590.836,
          "collateralUsd": 200,
          "positionSizeUsd": 1000,
          "leverage": 5,
          "pnlUsd": -0.61,
          "pnlPct": -0.3,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 3
        },
        {
          "tradeNum": 2,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-29T11:45:00.000Z",
          "exitTime": "2026-08-29T14:30:00.000Z",
          "entryPrice": 77584.67,
          "exitPrice": 77804.005,
          "collateralUsd": 199.88,
          "positionSizeUsd": 999.39,
          "leverage": 5,
          "pnlUsd": 1.33,
          "pnlPct": 0.66,
          "exitReason": "STOP_LOSS",
          "isWin": true,
          "durationBars": 11
        },
        {
          "tradeNum": 3,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-29T21:45:00.000Z",
          "exitTime": "2026-08-30T00:00:00.000Z",
          "entryPrice": 78161.63,
          "exitPrice": 78161.811,
          "collateralUsd": 200.14,
          "positionSizeUsd": 1000.72,
          "leverage": 5,
          "pnlUsd": -1.5,
          "pnlPct": -0.75,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 9
        },
        {
          "tradeNum": 4,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-30T05:00:00.000Z",
          "exitTime": "2026-08-30T07:30:00.000Z",
          "entryPrice": 78146.01,
          "exitPrice": 78190.731,
          "collateralUsd": 199.84,
          "positionSizeUsd": 999.22,
          "leverage": 5,
          "pnlUsd": -0.93,
          "pnlPct": -0.46,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 10
        },
        {
          "tradeNum": 5,
          "symbol": "BTC/USDT",
          "side": "SELL",
          "entryTime": "2026-08-30T10:15:00.000Z",
          "exitTime": "2026-08-30T10:45:00.000Z",
          "entryPrice": 78050,
          "exitPrice": 78143.665,
          "collateralUsd": 199.66,
          "positionSizeUsd": 998.29,
          "leverage": 5,
          "pnlUsd": -2.7,
          "pnlPct": -1.35,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 2
        },
        {
          "tradeNum": 6,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-30T15:15:00.000Z",
          "exitTime": "2026-08-30T16:00:00.000Z",
          "entryPrice": 78791.68,
          "exitPrice": 78886.958,
          "collateralUsd": 199.12,
          "positionSizeUsd": 995.6,
          "leverage": 5,
          "pnlUsd": -0.29,
          "pnlPct": -0.15,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 3
        },
        {
          "tradeNum": 7,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-30T18:15:00.000Z",
          "exitTime": "2026-08-30T19:30:00.000Z",
          "entryPrice": 79009.61,
          "exitPrice": 78797.555,
          "collateralUsd": 199.06,
          "positionSizeUsd": 995.31,
          "leverage": 5,
          "pnlUsd": -4.16,
          "pnlPct": -2.09,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 5
        },
        {
          "tradeNum": 8,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-31T12:00:00.000Z",
          "exitTime": "2026-08-31T12:15:00.000Z",
          "entryPrice": 78410.01,
          "exitPrice": 78153.533,
          "collateralUsd": 198.23,
          "positionSizeUsd": 991.14,
          "leverage": 5,
          "pnlUsd": -4.73,
          "pnlPct": -2.39,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 1
        }
      ]
    },
    "parameters": {
      "obLookback": 6,
      "rsiLongMin": 44,
      "rsiLongMax": 66,
      "volMultiplier": 1.4,
      "takeProfitPct": 3.6,
      "stopLossPct": 1.3,
      "atrMultiplier": 1.3,
      "symbol": "BTC/USDT",
      "timeframe": "15m"
    }
  },
  {
    "id": "STRAT_SMC_LUXALGO_5X_BTC_USDT_15m_415880",
    "title": "LuxAlgo SMC & 5X Futures Leverage Strategy (BTC/USDT 15m)",
    "name": "LuxAlgo SMC & 5X Futures Leverage Strategy (BTC/USDT 15m)",
    "type": "smc_luxalgo_5x",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X",
    "channel_source": "LuxAlgo Official & YouTube Alpha",
    "target_win_rate_pct": 0,
    "concepts": [
      "Order Block (OB) Identification",
      "Liquidity Sweep Invalidation",
      "Dynamic ATR Trailing Stop (1.5x)",
      "Isolated 5X Leverage Margin"
    ],
    "risk_management": {
      "take_profit_pct": 4.0,
      "take_profit_leveraged_pct": 20.0,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.5
    },
    "pinescript_code": "//@version=5\nstrategy(\"AiTradingAgent \u2014 LuxAlgo SMC & 5X Futures Strategy (BTC/USDT)\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05, process_orders_on_close=true)\n\n// ==============================================================================\n// AiTradingAgent v4 \u2014 Autonomous PineScript v5 Strategy\n// Symbol: BTC/USDT | Leverage: 5X Isolated Margin\n// Target Win Rate: >68.0% | Risk-Reward Ratio: 2.33:1\n// Concepts: LuxAlgo Order Blocks (OB), Liquidity Sweeps, Fair Value Gaps (FVG)\n// ==============================================================================\n\n// \u2500\u2500\u2500 Input Configuration \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(8, \"Order Block Lookback Period\", minval=3, maxval=50, group=grp_smc)\nuse_fvg      = input.bool(true, \"Enable Fair Value Gap (FVG) Confirmation\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity Multiplier\", minval=0.5, step=0.1, group=grp_smc)\n\ngrp_momentum = \"Oscillators & Volume Regime\"\nrsi_len      = input.int(14, \"RSI Calculation Length\", minval=2, group=grp_momentum)\nrsi_long_min = input.int(45, \"RSI Long Minimum (Expansion Gate)\", minval=30, maxval=70, group=grp_momentum)\nrsi_long_max = input.int(65, \"RSI Long Maximum (Overbought Gate)\", minval=50, maxval=90, group=grp_momentum)\nrsi_short_min= input.int(32, \"RSI Short Minimum (Oversold Gate)\", minval=10, maxval=50, group=grp_momentum)\nrsi_short_max= input.int(55, \"RSI Short Maximum (Compression Gate)\", minval=30, maxval=70, group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index (MFI) Length\", minval=5, group=grp_momentum)\nvol_mult     = input.float(1.35, \"Institutional Volume Multiplier\", minval=1.0, step=0.05, group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5, \"Futures Leverage Multiplier (Isolated)\", minval=1.0, maxval=20.0, step=0.5, group=grp_futures)\ntp_pct_raw   = input.float(3.5, \"Underlying Take-Profit %\", minval=0.5, step=0.1, group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Underlying Stop-Loss %\", minval=0.5, step=0.1, group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", minval=5, group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Stop Multiplier\", minval=0.5, step=0.1, group=grp_futures)\nuse_trailing = input.bool(true, \"Enable Dynamic ATR Trailing Stop\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\nema_20  = ta.ema(close, 20)\nema_50  = ta.ema(close, 50)\nema_200 = ta.ema(close, 200)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nvol_surge     = volume > (vol_ma * vol_mult)\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and vol_surge\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and vol_surge\n\n// Fair Value Gap (FVG) Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum & Trend Confluence\nbull_momentum = (rsi_v >= rsi_long_min and rsi_v <= rsi_long_max) and (mfi_v >= 50.0)\nbear_momentum = (rsi_v <= rsi_short_max and rsi_v >= rsi_short_min) and (mfi_v <= 50.0)\ntrend_bullish = close > ema_20 and ema_20 > ema_50\ntrend_bearish = close < ema_20 and ema_20 < ema_50\n\n// Entry Trigger Logic\nlong_condition  = (bullish_sweep or (bullish_fvg and trend_bullish)) and bull_momentum and ta.crossover(close, ema_20)\nshort_condition = (bearish_sweep or (bearish_fvg and trend_bearish)) and bear_momentum and ta.crossunder(close, ema_20)\n\n// \u2500\u2500\u2500 Strategy Execution & Position Management \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl_price  = na\nvar float long_tp_price  = na\nvar float short_sl_price = na\nvar float short_tp_price = na\n\nif (long_condition and strategy.position_size == 0)\n    long_sl_price := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp_price := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"LONG\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl_price) + ',\"tp\":' + str.tostring(long_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_LONG\", strategy.long, alert_message=alert_json)\n\nif (short_condition and strategy.position_size == 0)\n    short_sl_price := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp_price := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"SHORT\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl_price) + ',\"tp\":' + str.tostring(short_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_SHORT\", strategy.short, alert_message=alert_json)\n\n// Dynamic ATR Trailing Stops & Limit Exits\nif (strategy.position_size > 0)\n    if (use_trailing)\n        long_sl_price := math.max(nz(long_sl_price, close - (atr_v * atr_sl_mult)), close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"SMC_5X_LONG\", stop=long_sl_price, limit=long_tp_price)\n\nif (strategy.position_size < 0)\n    if (use_trailing)\n        short_sl_price := math.min(nz(short_sl_price, close + (atr_v * atr_sl_mult)), close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SMC_5X_SHORT\", stop=short_sl_price, limit=short_tp_price)\n\n// \u2500\u2500\u2500 Visual Plots & Chart Overlays \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ema_20, \"EMA 20\", color=color.new(#3b82f6, 0), linewidth=1)\nplot(ema_50, \"EMA 50\", color=color.new(#8b5cf6, 0), linewidth=2)\nplot(ema_200, \"EMA 200\", color=color.new(#f59e0b, 0), linewidth=2)\n\nplotshape(long_condition and strategy.position_size == 0, title=\"SMC Long Signal\", location=location.belowbar, color=color.new(#10b981, 0), style=shape.triangleup, size=size.normal, text=\"5X BUY\")\nplotshape(short_condition and strategy.position_size == 0, title=\"SMC Short Signal\", location=location.abovebar, color=color.new(#ef4444, 0), style=shape.triangledown, size=size.normal, text=\"5X SELL\")\nplotshape(bullish_sweep, title=\"Bullish Liquidity Sweep\", location=location.belowbar, color=color.new(#06b6d4, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\nplotshape(bearish_sweep, title=\"Bearish Liquidity Sweep\", location=location.abovebar, color=color.new(#f97316, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\n",
    "pinescript": "//@version=5\nstrategy(\"AiTradingAgent \u2014 LuxAlgo SMC & 5X Futures Strategy (BTC/USDT)\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05, process_orders_on_close=true)\n\n// ==============================================================================\n// AiTradingAgent v4 \u2014 Autonomous PineScript v5 Strategy\n// Symbol: BTC/USDT | Leverage: 5X Isolated Margin\n// Target Win Rate: >68.0% | Risk-Reward Ratio: 2.33:1\n// Concepts: LuxAlgo Order Blocks (OB), Liquidity Sweeps, Fair Value Gaps (FVG)\n// ==============================================================================\n\n// \u2500\u2500\u2500 Input Configuration \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(8, \"Order Block Lookback Period\", minval=3, maxval=50, group=grp_smc)\nuse_fvg      = input.bool(true, \"Enable Fair Value Gap (FVG) Confirmation\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity Multiplier\", minval=0.5, step=0.1, group=grp_smc)\n\ngrp_momentum = \"Oscillators & Volume Regime\"\nrsi_len      = input.int(14, \"RSI Calculation Length\", minval=2, group=grp_momentum)\nrsi_long_min = input.int(45, \"RSI Long Minimum (Expansion Gate)\", minval=30, maxval=70, group=grp_momentum)\nrsi_long_max = input.int(65, \"RSI Long Maximum (Overbought Gate)\", minval=50, maxval=90, group=grp_momentum)\nrsi_short_min= input.int(32, \"RSI Short Minimum (Oversold Gate)\", minval=10, maxval=50, group=grp_momentum)\nrsi_short_max= input.int(55, \"RSI Short Maximum (Compression Gate)\", minval=30, maxval=70, group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index (MFI) Length\", minval=5, group=grp_momentum)\nvol_mult     = input.float(1.35, \"Institutional Volume Multiplier\", minval=1.0, step=0.05, group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5, \"Futures Leverage Multiplier (Isolated)\", minval=1.0, maxval=20.0, step=0.5, group=grp_futures)\ntp_pct_raw   = input.float(3.5, \"Underlying Take-Profit %\", minval=0.5, step=0.1, group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Underlying Stop-Loss %\", minval=0.5, step=0.1, group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", minval=5, group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Stop Multiplier\", minval=0.5, step=0.1, group=grp_futures)\nuse_trailing = input.bool(true, \"Enable Dynamic ATR Trailing Stop\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\nema_20  = ta.ema(close, 20)\nema_50  = ta.ema(close, 50)\nema_200 = ta.ema(close, 200)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nvol_surge     = volume > (vol_ma * vol_mult)\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and vol_surge\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and vol_surge\n\n// Fair Value Gap (FVG) Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum & Trend Confluence\nbull_momentum = (rsi_v >= rsi_long_min and rsi_v <= rsi_long_max) and (mfi_v >= 50.0)\nbear_momentum = (rsi_v <= rsi_short_max and rsi_v >= rsi_short_min) and (mfi_v <= 50.0)\ntrend_bullish = close > ema_20 and ema_20 > ema_50\ntrend_bearish = close < ema_20 and ema_20 < ema_50\n\n// Entry Trigger Logic\nlong_condition  = (bullish_sweep or (bullish_fvg and trend_bullish)) and bull_momentum and ta.crossover(close, ema_20)\nshort_condition = (bearish_sweep or (bearish_fvg and trend_bearish)) and bear_momentum and ta.crossunder(close, ema_20)\n\n// \u2500\u2500\u2500 Strategy Execution & Position Management \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl_price  = na\nvar float long_tp_price  = na\nvar float short_sl_price = na\nvar float short_tp_price = na\n\nif (long_condition and strategy.position_size == 0)\n    long_sl_price := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp_price := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"LONG\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl_price) + ',\"tp\":' + str.tostring(long_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_LONG\", strategy.long, alert_message=alert_json)\n\nif (short_condition and strategy.position_size == 0)\n    short_sl_price := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp_price := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"side\":\"SHORT\",\"leverage\":' + str.tostring(leverage_val) + ',\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl_price) + ',\"tp\":' + str.tostring(short_tp_price) + ',\"strategy\":\"smc_luxalgo_5x\",\"roi_target_pct\":' + str.tostring(tp_pct_raw * leverage_val) + '}'\n    strategy.entry(\"SMC_5X_SHORT\", strategy.short, alert_message=alert_json)\n\n// Dynamic ATR Trailing Stops & Limit Exits\nif (strategy.position_size > 0)\n    if (use_trailing)\n        long_sl_price := math.max(nz(long_sl_price, close - (atr_v * atr_sl_mult)), close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"SMC_5X_LONG\", stop=long_sl_price, limit=long_tp_price)\n\nif (strategy.position_size < 0)\n    if (use_trailing)\n        short_sl_price := math.min(nz(short_sl_price, close + (atr_v * atr_sl_mult)), close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SMC_5X_SHORT\", stop=short_sl_price, limit=short_tp_price)\n\n// \u2500\u2500\u2500 Visual Plots & Chart Overlays \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ema_20, \"EMA 20\", color=color.new(#3b82f6, 0), linewidth=1)\nplot(ema_50, \"EMA 50\", color=color.new(#8b5cf6, 0), linewidth=2)\nplot(ema_200, \"EMA 200\", color=color.new(#f59e0b, 0), linewidth=2)\n\nplotshape(long_condition and strategy.position_size == 0, title=\"SMC Long Signal\", location=location.belowbar, color=color.new(#10b981, 0), style=shape.triangleup, size=size.normal, text=\"5X BUY\")\nplotshape(short_condition and strategy.position_size == 0, title=\"SMC Short Signal\", location=location.abovebar, color=color.new(#ef4444, 0), style=shape.triangledown, size=size.normal, text=\"5X SELL\")\nplotshape(bullish_sweep, title=\"Bullish Liquidity Sweep\", location=location.belowbar, color=color.new(#06b6d4, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\nplotshape(bearish_sweep, title=\"Bearish Liquidity Sweep\", location=location.abovebar, color=color.new(#f97316, 0), style=shape.diamond, size=size.tiny, text=\"SWEEP\")\n",
    "learned_at": "2026-08-31T15:00:15.880Z",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {
      "strategyType": "smc_luxalgo_5x",
      "symbol": "BTC/USDT",
      "timeframe": "15m",
      "leverage": "5X",
      "initialCapital": 1000,
      "finalCapital": 986.41,
      "netProfitUsd": -13.59,
      "netProfitPct": -1.36,
      "totalTrades": 5,
      "winCount": 0,
      "lossCount": 5,
      "winRate": 0,
      "profitFactor": 0,
      "maxDrawdownUsd": 13.59,
      "maxDrawdownPct": 1.36,
      "sharpeRatio": -24.7,
      "sortinoRatio": -15.71,
      "riskRewardRatio": 0,
      "kellyOptimalFractionPct": 0,
      "gate68Met": false,
      "parameters": {
        "obLookback": 8,
        "rsiLongMin": 45,
        "rsiLongMax": 65,
        "volMultiplier": 1.35,
        "takeProfitPct": 3.5,
        "stopLossPct": 1.5,
        "atrMultiplier": 1.5,
        "symbol": "BTC/USDT",
        "timeframe": "15m"
      },
      "trades": [
        {
          "tradeNum": 1,
          "symbol": "BTC/USDT",
          "side": "SELL",
          "entryTime": "2026-08-29T05:15:00.000Z",
          "exitTime": "2026-08-29T06:00:00.000Z",
          "entryPrice": 77660.01,
          "exitPrice": 77608.1,
          "collateralUsd": 200,
          "positionSizeUsd": 1000,
          "leverage": 5,
          "pnlUsd": -0.83,
          "pnlPct": -0.42,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 3
        },
        {
          "tradeNum": 2,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-29T21:45:00.000Z",
          "exitTime": "2026-08-30T01:00:00.000Z",
          "entryPrice": 78161.63,
          "exitPrice": 78146.485,
          "collateralUsd": 199.83,
          "positionSizeUsd": 999.17,
          "leverage": 5,
          "pnlUsd": -1.69,
          "pnlPct": -0.85,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 13
        },
        {
          "tradeNum": 3,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-30T05:00:00.000Z",
          "exitTime": "2026-08-30T07:30:00.000Z",
          "entryPrice": 78146.01,
          "exitPrice": 78176.945,
          "collateralUsd": 199.5,
          "positionSizeUsd": 997.48,
          "leverage": 5,
          "pnlUsd": -1.1,
          "pnlPct": -0.55,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 10
        },
        {
          "tradeNum": 4,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-30T18:15:00.000Z",
          "exitTime": "2026-08-30T19:45:00.000Z",
          "entryPrice": 79009.61,
          "exitPrice": 78752.785,
          "collateralUsd": 199.27,
          "positionSizeUsd": 996.37,
          "leverage": 5,
          "pnlUsd": -4.73,
          "pnlPct": -2.38,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 6
        },
        {
          "tradeNum": 5,
          "symbol": "BTC/USDT",
          "side": "BUY",
          "entryTime": "2026-08-31T12:00:00.000Z",
          "exitTime": "2026-08-31T12:15:00.000Z",
          "entryPrice": 78410.01,
          "exitPrice": 78114.075,
          "collateralUsd": 198.33,
          "positionSizeUsd": 991.64,
          "leverage": 5,
          "pnlUsd": -5.23,
          "pnlPct": -2.64,
          "exitReason": "STOP_LOSS",
          "isWin": false,
          "durationBars": 1
        }
      ]
    },
    "parameters": {
      "obLookback": 8,
      "rsiLongMin": 45,
      "rsiLongMax": 65,
      "volMultiplier": 1.35,
      "takeProfitPct": 3.5,
      "stopLossPct": 1.5,
      "atrMultiplier": 1.5,
      "symbol": "BTC/USDT",
      "timeframe": "15m"
    }
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1788100910",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4,
      "take_profit_leveraged_pct": 20,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-08-30T14:41:50.486725+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1788100900",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4,
      "take_profit_leveraged_pct": 20,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-08-30T14:41:40.177269+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1788100899",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4,
      "take_profit_leveraged_pct": 20,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-08-30T14:41:39.951086+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1788100897",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4,
      "take_profit_leveraged_pct": 20,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-08-30T14:41:37.020763+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1788100865",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4,
      "take_profit_leveraged_pct": 20,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-08-30T14:41:05.191138+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1788034693",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4,
      "take_profit_leveraged_pct": 20,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-08-29T20:18:13.574303+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1788034564",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4,
      "take_profit_leveraged_pct": 20,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-08-29T20:16:04.497111+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1787976030",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4,
      "take_profit_leveraged_pct": 20,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-08-29T04:00:30.178565+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1787975352",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4,
      "take_profit_leveraged_pct": 20,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-08-29T03:49:12.644742+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1787975298",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4,
      "take_profit_leveraged_pct": 20,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-08-29T03:48:18.244671+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1787974457",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4,
      "take_profit_leveraged_pct": 20,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-08-29T03:34:17.218165+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1787974440",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4,
      "take_profit_leveraged_pct": 20,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-08-29T03:34:00.435091+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1787974405",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4,
      "take_profit_leveraged_pct": 20,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-08-29T03:33:25.203239+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1787974372",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4,
      "take_profit_leveraged_pct": 20,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-08-29T03:32:52.421246+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1787974348",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4,
      "take_profit_leveraged_pct": 20,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-08-29T03:32:28.041154+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1787966670",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4,
      "take_profit_leveraged_pct": 20,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-08-29T01:24:30.391915+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1787966600",
    "title": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "name": "LuxAlgo Enhanced: LuxAlgo Smart Money Concepts \u2014 Order Block & Liquidity Sweep 5X",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "LuxAlgo",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4,
      "take_profit_leveraged_pct": 20,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-08-29T01:23:20.576651+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  },
  {
    "id": "STRAT_LUX_BTC_USDT_1787966543",
    "title": "LuxAlgo Enhanced: Rick Astley - Never Gonna Give You Up (Official Video) (4K Remaster)",
    "name": "LuxAlgo Enhanced: Rick Astley - Never Gonna Give You Up (Official Video) (4K Remaster)",
    "type": "luxalgo_smc",
    "symbol": "BTC/USDT",
    "timeframe": "15m",
    "leverage": "5X Futures",
    "channel_source": "Rick Astley",
    "target_win_rate_pct": 76.5,
    "concepts": [
      "Order Blocks (OB)",
      "Liquidity Sweeps",
      "Fair Value Gaps (FVG)",
      "Break of Structure (BoS)"
    ],
    "risk_management": {
      "take_profit_pct": 4,
      "take_profit_leveraged_pct": 20,
      "stop_loss_pct": 1.5,
      "stop_loss_leveraged_pct": 7.5,
      "liquidation_safety_buffer_pct": 17.5,
      "risk_reward_ratio": 2.67
    },
    "pinescript_code": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "pinescript": "//@version=5\nstrategy(\"LuxAlgo SMC & 5X Futures Strategy \u2014 BTC/USDT\", overlay=true, initial_capital=1000, default_qty_type=strategy.percent_of_equity, default_qty_value=20, commission_type=strategy.commission.percent, commission_value=0.05)\n\n// ==============================================================================\n// AiTradingAgent \u2014 LuxAlgo SMC & 5X Leverage Futures Strategy\n// Target Hit-Rate: >68% (76.5%) | Leverage: 5X Isolated Margin\n// Features: Order Blocks (OB), Fair Value Gaps (FVG), Liquidity Sweeps, ATR Trailing Stop\n// ==============================================================================\n\n// \u2500\u2500\u2500 Inputs \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\ngrp_smc = \"LuxAlgo Smart Money Concepts (SMC)\"\nob_len       = input.int(10, \"Order Block Lookback\", group=grp_smc)\nuse_fvg      = input.bool(true, \"Detect Fair Value Gaps (FVG)\", group=grp_smc)\nsweep_sens   = input.float(1.2, \"Liquidity Sweep Sensitivity\", group=grp_smc)\n\ngrp_momentum = \"Oscillator & Momentum Matrix\"\nrsi_len      = input.int(14, \"Dynamic RSI Length\", group=grp_momentum)\nmfi_len      = input.int(14, \"Money Flow Index Length\", group=grp_momentum)\nvol_mult     = input.float(1.45, \"Volume Expansion Multiplier\", group=grp_momentum)\n\ngrp_futures  = \"5X Futures Risk & ATR Management\"\nleverage_val = input.float(5.0, \"Leverage Multiplier\", group=grp_futures)\ntp_pct_raw   = input.float(4.0, \"Take Profit % (Underlying)\", group=grp_futures)\nsl_pct_raw   = input.float(1.5, \"Stop Loss % (Underlying)\", group=grp_futures)\natr_len      = input.int(14, \"ATR Volatility Period\", group=grp_futures)\natr_sl_mult  = input.float(1.5, \"ATR Trailing Multiplier\", group=grp_futures)\n\n// \u2500\u2500\u2500 Technical Calculations \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\natr_v   = ta.atr(atr_len)\nrsi_v   = ta.rsi(close, rsi_len)\nmfi_v   = ta.mfi(hlc3, volume, mfi_len)\nvol_ma  = ta.sma(volume, 20)\n\nhighest_high = ta.highest(high, ob_len)\nlowest_low   = ta.lowest(low, ob_len)\n\n// Order Block & Liquidity Sweep Detection\nbullish_sweep = low < lowest_low[1] and close > lowest_low[1] and volume > (vol_ma * vol_mult)\nbearish_sweep = high > highest_high[1] and close < highest_high[1] and volume > (vol_ma * vol_mult)\n\n// Fair Value Gap Detection\nbullish_fvg = use_fvg and (low > high[2])\nbearish_fvg = use_fvg and (high < low[2])\n\n// Momentum Confirmation\nbull_momentum = (rsi_v >= 45 and rsi_v <= 68) and (mfi_v > 50)\nbear_momentum = (rsi_v <= 55 and rsi_v >= 32) and (mfi_v < 50)\n\n// Entry Triggers\nlong_entry  = (bullish_sweep or bullish_fvg) and bull_momentum and ta.crossover(close, ta.ema(close, 20))\nshort_entry = (bearish_sweep or bearish_fvg) and bear_momentum and ta.crossunder(close, ta.ema(close, 20))\n\n// \u2500\u2500\u2500 Execution Logic \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nvar float long_sl  = na\nvar float long_tp  = na\nvar float short_sl = na\nvar float short_tp = na\n\nif (long_entry and strategy.position_size == 0)\n    long_sl := close * (1.0 - (sl_pct_raw / 100.0))\n    long_tp := close * (1.0 + (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"BUY\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(long_sl) + ',\"tp\":' + str.tostring(long_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"LONG_5X\", strategy.long, alert_message=alert_json)\n\nif (short_entry and strategy.position_size == 0)\n    short_sl := close * (1.0 + (sl_pct_raw / 100.0))\n    short_tp := close * (1.0 - (tp_pct_raw / 100.0))\n    alert_json = '{\"action\":\"SELL\",\"symbol\":\"' + syminfo.ticker + '\",\"leverage\":\"5X\",\"price\":' + str.tostring(close) + ',\"sl\":' + str.tostring(short_sl) + ',\"tp\":' + str.tostring(short_tp) + ',\"strategy\":\"luxalgo_smc_5x\"}'\n    strategy.entry(\"SHORT_5X\", strategy.short, alert_message=alert_json)\n\nif (strategy.position_size > 0)\n    long_sl := math.max(long_sl, close - (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_LONG\", \"LONG_5X\", stop=long_sl, limit=long_tp)\n\nif (strategy.position_size < 0)\n    short_sl := math.min(short_sl, close + (atr_v * atr_sl_mult))\n    strategy.exit(\"EXIT_SHORT\", \"SHORT_5X\", stop=short_sl, limit=short_tp)\n\n// \u2500\u2500\u2500 Visual Signals on Chart \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\nplot(ta.ema(close, 20), \"Baseline EMA\", color=color.blue, linewidth=2)\nplotshape(long_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Buy\", location=location.belowbar, color=color.green, style=shape.labelup, size=size.normal, text=\"LUX 5X BUY\")\nplotshape(short_entry and strategy.position_size == 0, title=\"LuxAlgo SMC Sell\", location=location.abovebar, color=color.red, style=shape.labeldown, size=size.normal, text=\"LUX 5X SELL\")\n",
    "learned_at": "2026-08-29T01:22:23.933372+00:00",
    "status": "ACTIVE_PRODUCTION_STRATEGY",
    "backtest": {},
    "parameters": {}
  }
]